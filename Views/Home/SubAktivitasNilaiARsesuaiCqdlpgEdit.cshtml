@{
    // View
    View = this;

    // Check action result
    if (subAktivitasNilaiARsesuaiCqdlpgEdit.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = subAktivitasNilaiARsesuaiCqdlpgEdit.UseLayout ? "_Layout" : "";
}
@subAktivitasNilaiARsesuaiCqdlpgEdit.ShowPageHeader()
@subAktivitasNilaiARsesuaiCqdlpgEdit.ShowMessages()
<main class="edit">
<form name="fSubAktivitasNilaiARsesuaiCQDLPGedit" id="fSubAktivitasNilaiARsesuaiCQDLPGedit" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.FormClassName)" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
<script>
var currentTable = @Html.Raw(ConvertToJson(subAktivitasNilaiARsesuaiCqdlpgEdit.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { SubAktivitasNilaiARsesuaiCQDLPG: currentTable } });
var currentPageID = ew.PAGE_ID = "edit";
var currentForm;
var fSubAktivitasNilaiARsesuaiCQDLPGedit;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fSubAktivitasNilaiARsesuaiCQDLPGedit")
        .setPageId("edit")

        // Add fields
        .setFields([

            ["idProses", [fields.idProses.visible && fields.idProses.required ? ew.Validators.required(fields.idProses.caption) : null], fields.idProses.isInvalid],
            ["idAktifitas", [fields.idAktifitas.visible && fields.idAktifitas.required ? ew.Validators.required(fields.idAktifitas.caption) : null], fields.idAktifitas.isInvalid],
            ["StatusAktivitas", [fields.StatusAktivitas.visible && fields.StatusAktivitas.required ? ew.Validators.required(fields.StatusAktivitas.caption) : null], fields.StatusAktivitas.isInvalid],
            ["NoReferensi", [fields.NoReferensi.visible && fields.NoReferensi.required ? ew.Validators.required(fields.NoReferensi.caption) : null], fields.NoReferensi.isInvalid],
            ["LastUpdatedBy", [fields.LastUpdatedBy.visible && fields.LastUpdatedBy.required ? ew.Validators.required(fields.LastUpdatedBy.caption) : null], fields.LastUpdatedBy.isInvalid],
            ["lastUpdatedDate", [fields.lastUpdatedDate.visible && fields.lastUpdatedDate.required ? ew.Validators.required(fields.lastUpdatedDate.caption) : null], fields.lastUpdatedDate.isInvalid],
            ["MT", [fields.MT.visible && fields.MT.required ? ew.Validators.required(fields.MT.caption) : null], fields.MT.isInvalid]
        ])

        // Form_CustomValidate
        .setCustomValidate(
            function(fobj) { // DO NOT CHANGE THIS LINE!
                    // Your custom validation code here, return false if invalid.
                    return true;
                }
        )

        // Use JavaScript validation or not
        .setValidateRequired(ew.CLIENT_VALIDATE)

        // Dynamic selection lists
        .setLists({
        })
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Client script
    // Write your client script here, no need to add script tags.
    // Write your client script here, no need to add script tags.
    var parent_url = ew.PATH_BASE;
    var NoReferensi = $("#x_NoReferensi").val();
    var IdProses = $("#x_idProses").val();
    var IdAktivitas = $("#x_idAktifitas").val();
    let proceedSubmitStatus = null;
    $('#x_MT').on('focus', function () {
        hideFeedback($(this));
    });
    function hideFeedback(el) {
        let feedback = el.siblings('.invalid-feedback');
        feedback.hide();
    }
    function validasiRob() {
            const robFields = [
                "x_MT"
            ];
            var inputMap = {
                "#x_MT": "AR Mt"
            };
            let isValid = true;
            $('.invalid-feedback').hide();
            let zeroOrNegativeFields = [];
            for (var id in inputMap) {
                var el = $(id);
                var label = inputMap[id];
                var val = el.val().trim();
                el.val(val);
                var feedback = el.siblings('.invalid-feedback');
                if (val === "") {
                    feedback.text('*' + label + ' tidak boleh kosong.').show();
                    zeroOrNegativeFields.push(label);
                    isValid = false;
                    continue;
                }
                if (isNaN(val)) {
                    feedback.text('*' + label + ' harus diisi dengan angka desimal yang valid.').show();
                    zeroOrNegativeFields.push(label);
                    isValid = false;
                    continue;
                }
                var number = parseFloat(val);
                if (number <= 0) {
                    zeroOrNegativeFields.push(label);
                    // feedback.text('*' + label + ' tidak boleh bernilai 0.').show();
                    //     isValid = false;
                }  
            }      

                // Jika dicentang, cek semua field harus terisi
            let fieldNotValid = [];
            for (const fieldId in inputMap) {
                const el = $(fieldId);
                const label = inputMap[fieldId];
                if (el.length && !el.val().trim()) {
                    fieldNotValid.push(label);
                    isValid = false
                }
            }
            if (!isValid) {
                ew.alert("Field tidak boleh kosong: " + fieldNotValid.join(", "));
                return { isValid: false}; // Gagal submit
            } else {
                $('.invalid-feedback').hide();
                if (zeroOrNegativeFields.length > 0) {
                    return { isValid: false, hasNegative: true, fields: zeroOrNegativeFields };
                }
                return { isValid: true};
            }
        return { isValid: true};
    }
    function checkAndSubmit(status) {
        let sfbdValue = parseFloat($("#td_SFBD_Mt").text());

        // Jika tidak angka, atau kosong
        if (isNaN(sfbdValue) || sfbdValue === null || sfbdValue === "") {
            ew.alert("Nilai SFBD belum tersedia, mohon dilengkapi terlebih dahulu");
            return;
        }
        const robResult = validasiRob();
        if (!robResult.isValid && !robResult.hasNegative) return;
        let allNegativeFields = [];
        if (robResult.hasNegative) allNegativeFields = allNegativeFields.concat(robResult.fields);
        if (allNegativeFields.length > 0) {
            let warningText = "Field berikut bernilai 0 atau kurang: " + allNegativeFields.join(", ") + ". Apakah Anda ingin tetap menyimpan?";
            $("#warningValueText").text(warningText);
            proceedSubmitStatus = status;
            $("#warningValueModal").modal("show");
            return;
        }

        // Jika nilai valid langsung submit
        handleCustomSubmit(status);
    }
    function handleCustomSubmit(status) {
        let idAktivitas = $("#x_idAktifitas").val();
        const originalStatus = document.querySelector('#el_SubAktivitasNilaiARsesuaiCQDLPG_StatusAktivitas .form-control-plaintext').textContent;
        let sfbdValue = $("#td_SFBD_Mt").text();
        if (sfbdValue == "" || sfbdValue == null) {
            ew.alert("Nilai SFBD belum tersedia, mohon dilengkapi terlebih dahulu");
            return;
        }
        $.ajax({
            url: parent_url + `api/LookUpList/check_valid_status_aktivitas?idAktivitas=${idAktivitas}&originalStatus=${originalStatus}`,
            type: 'GET',
            beforeSend: function () {
                showLoading();
            },
            success: function (response) {
                if (!response.IsValid){
                    hideLoading();
                    ew.alert(response.Message ?? "Status aktivitas telah diubah oleh pengguna lain");
                    return;
                } else {
                   let form = $("#fSubAktivitasNilaiARsesuaiCQDLPGedit");
                    $.ajax({
                        url: form.attr("action"),
                        type: form.attr("method"),
                        data: form.serialize(),
                        success: function (response) {
                            updateStatus(idAktivitas, status);
                        },
                        error: function (xhr, status, error) {
                            hideLoading();
                            ew.alert("Gagal menyimpan data.");
                            console.error("Form submit gagal:", error);
                        }
                    });  
                }
            },
            error: function (xhr, status, error) {
                hideLoading();
                ew.alert("Gagal menyimpan data.");
                console.error("Form submit gagal:", error);
            }
        }); 
    }
    function updateStatus(idAktivitas, status) {
        $.ajax({
            url: parent_url + `api/LookUpList/update_status_aktivitas?idAktivitas=${idAktivitas}&statusAktivitas=${status}`,
            type: 'POST',
            success: function () {
                console.log("Status berhasil diperbarui.");
                if (status == "Completed" || status == "Completed Edit") {
                    $.ajax({
                        url: parent_url + 'api/LookUpList/update_lop_r3_r4',
                        type: 'POST',
                        data: JSON.stringify({
                            NoReferensi: NoReferensi,
                            IdProses: IdProses,
                            IdAktivitas: IdAktivitas,
                            TipeProduk: 'LPG'
                        }),
                        contentType: 'application/json',
                        success: function (res) {
                            window.location.href = `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`;
                        },
                        error: function (xhr, status, error) {
                            console.error("Update Aktivitas Dokumen gagal:", error);
                        },
                        complete: function () {
                            hideLoading();
                        }
                    });
                } else {
                    hideLoading();
                    location.reload();
                }
            },
            error: function (xhr, status, error) {
                ew.alert("Status gagal diperbarui.");
                console.error("Update status gagal:", error);
                hideLoading();
            }
        });
    }
    function reverseStatus(status, catatan) {
        showLoading();
        let idAktivitas = $("#x_idAktifitas").val();
        $.ajax({
                url: parent_url + `api/LookUpList/update_status_aktivitas-v2`,
                type: 'POST',
                data: JSON.stringify({ IdAktivitas: idAktivitas, StatusAktivitas: status, Catatan: catatan ?? null }),
                contentType: 'application/json',
                success: function () {
                    $.ajax({
                        url: parent_url + 'api/LookUpList/generate_etc',
                        method: 'POST',
                        data: JSON.stringify({ NoReferensi: NoReferensi }),
                        contentType: 'application/json',
                        success: function (res) {
                            setTimeout(function () {
                                window.location.href = `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`;
                            }, 1000);
                        },
                        error: function (err) {
                            hideLoading();
                            ew.alert('Perubahan gagal disimpan.');
                            console.error(err);
                        },
                    });
                },
                error: function (xhr, status, error) {
                    ew.alert("Status gagal diperbarui.");
                    console.error("Update status gagal:", error);
                },
                complete: function () {
                    hideLoading();
                }
            });
        }
    $(document).on("submit", "#reverseStatusEditingModalForm", function (e) {
        e.preventDefault();
        let catatanText = $("#catatanReverseStatusEditing").val();
        reverseStatus("Editing", catatanText);
    });
    $(document).on("click", "#btn-draft", function () {
        checkAndSubmit("Draft");
    });
    $(document).on("click", "#btn-completed", function () {
        checkAndSubmit("Completed");
    });
    $(document).on("click", "#btn-editing", function () {
        checkAndSubmit("Editing");
    });
    $(document).on("click", "#btn-completedEdit", function () {
        checkAndSubmit("Completed Edit");
    });
    $(document).on("click", "#btn-confirm-modal", function () {
        $("#warningValueModal").modal("hide");
        handleCustomSubmit(proceedSubmitStatus);
    });
});
</script>
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="update">
<input type="hidden" name="modal" value="@ConvertToInt(subAktivitasNilaiARsesuaiCqdlpgEdit.IsModal)">
@if (IsJsonResponse()) {
<input type="hidden" name="json" value="1">
}
<input type="hidden" name="@(subAktivitasNilaiARsesuaiCqdlpgEdit.OldKeyName)" value="@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.OldKey)">
<div class="ew-edit-div"><!-- page* -->
@if (subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.Visible) { // subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.Visible (begin)
    <div id="r_idProses" @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.RowAttributes)>
        <label id="elh_SubAktivitasNilaiARsesuaiCQDLPG_idProses" for="x_idProses" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.Caption)@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.CellAttributes)>
<span id="el_SubAktivitasNilaiARsesuaiCQDLPG_idProses">
<span @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.DisplayValue(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_idProses" data-hidden="1" name="x_idProses" id="x_idProses" value="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.Visible (end) *@
@if (subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.Visible) { // subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.Visible (begin)
    <div id="r_idAktifitas" @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.RowAttributes)>
        <label id="elh_SubAktivitasNilaiARsesuaiCQDLPG_idAktifitas" for="x_idAktifitas" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.Caption)@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.CellAttributes)>
<span id="el_SubAktivitasNilaiARsesuaiCQDLPG_idAktifitas">
<span @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.DisplayValue(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_idAktifitas" data-hidden="1" name="x_idAktifitas" id="x_idAktifitas" value="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.Visible (end) *@
@if (subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.Visible) { // subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.Visible (begin)
    <div id="r_StatusAktivitas" @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.RowAttributes)>
        <label id="elh_SubAktivitasNilaiARsesuaiCQDLPG_StatusAktivitas" for="x_StatusAktivitas" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.Caption)@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.CellAttributes)>
<span id="el_SubAktivitasNilaiARsesuaiCQDLPG_StatusAktivitas">
<span @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.DisplayValue(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_StatusAktivitas" data-hidden="1" name="x_StatusAktivitas" id="x_StatusAktivitas" value="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasNilaiARsesuaiCqdlpgEdit.StatusAktivitas.Visible (end) *@
@if (subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.Visible) { // subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.Visible (begin)
    <div id="r_NoReferensi" @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.RowAttributes)>
        <label id="elh_SubAktivitasNilaiARsesuaiCQDLPG_NoReferensi" for="x_NoReferensi" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.Caption)@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.CellAttributes)>
<span id="el_SubAktivitasNilaiARsesuaiCQDLPG_NoReferensi">
<span @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_NoReferensi" data-hidden="1" name="x_NoReferensi" id="x_NoReferensi" value="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.Visible (end) *@
@if (subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.Visible) { // subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.Visible (begin)
    <div id="r_LastUpdatedBy" @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.RowAttributes)>
        <label id="elh_SubAktivitasNilaiARsesuaiCQDLPG_LastUpdatedBy" for="x_LastUpdatedBy" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.Caption)@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.CellAttributes)>
<span id="el_SubAktivitasNilaiARsesuaiCQDLPG_LastUpdatedBy">
<span @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_LastUpdatedBy" data-hidden="1" name="x_LastUpdatedBy" id="x_LastUpdatedBy" value="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasNilaiARsesuaiCqdlpgEdit.LastUpdatedBy.Visible (end) *@
@if (subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.Visible) { // subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.Visible (begin)
    <div id="r_lastUpdatedDate" @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.RowAttributes)>
        <label id="elh_SubAktivitasNilaiARsesuaiCQDLPG_lastUpdatedDate" for="x_lastUpdatedDate" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.Caption)@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.CellAttributes)>
<span id="el_SubAktivitasNilaiARsesuaiCQDLPG_lastUpdatedDate">
<span @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_lastUpdatedDate" data-hidden="1" name="x_lastUpdatedDate" id="x_lastUpdatedDate" value="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasNilaiARsesuaiCqdlpgEdit.lastUpdatedDate.Visible (end) *@
@if (subAktivitasNilaiARsesuaiCqdlpgEdit.MT.Visible) { // subAktivitasNilaiARsesuaiCqdlpgEdit.MT.Visible (begin)
    <div id="r_MT" @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.RowAttributes)>
        <label id="elh_SubAktivitasNilaiARsesuaiCQDLPG_MT" for="x_MT" class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.Caption)@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.CellAttributes)>
<span id="el_SubAktivitasNilaiARsesuaiCQDLPG_MT">
<input type="@(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.InputTextType)" name="x_MT" id="x_MT" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_MT" value="@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.EditValue)" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.PlaceHolder))" data-format-pattern="@(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.FormatPattern)"@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.EditAttributes) aria-describedby="x_MT_help">
@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.MT.GetErrorMessage())</div>
</span>
</div></div>
    </div>
} @* subAktivitasNilaiARsesuaiCqdlpgEdit.MT.Visible (end) *@
</div><!-- /page* -->
    <input type="hidden" data-table="SubAktivitasNilaiARsesuaiCQDLPG" data-field="x_id" data-hidden="1" name="x_id" id="x_id" value="@Html.Raw(HtmlEncode(subAktivitasNilaiARsesuaiCqdlpgEdit.id.CurrentValue))">
@{ subAktivitasNilaiARsesuaiCqdlpgEdit.CloseRecordset(); }
@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.IsModal ? "<template class=\"ew-modal-buttons\">" : "<div class=\"row ew-buttons\">")<!-- buttons .row -->
    <div class="@(subAktivitasNilaiARsesuaiCqdlpgEdit.OffsetColumnClass)"><!-- buttons offset -->
    <button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit" form="fSubAktivitasNilaiARsesuaiCQDLPGedit">@Html.Raw(Language.Phrase("SaveBtn"))</button>
@if (IsJsonResponse()) {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-bs-dismiss="modal">@Html.Raw(Language.Phrase("CancelBtn"))</button>
} else {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" form="fSubAktivitasNilaiARsesuaiCQDLPGedit" data-href="@AppPath(subAktivitasNilaiARsesuaiCqdlpgEdit.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
}
    </div><!-- /buttons offset -->
@Html.Raw(subAktivitasNilaiARsesuaiCqdlpgEdit.IsModal ? "</template>" : "</div>")<!-- /buttons .row -->
</form>
</main>
@subAktivitasNilaiARsesuaiCqdlpgEdit.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("SubAktivitasNilaiARsesuaiCQDLPG");
});
</script>
<script>
loadjs.ready("load", function() {
    // Startup script
    // Write your table-specific startup script here
    var IdReferensi = "@subAktivitasNilaiARsesuaiCqdlpgEdit.NoReferensi.CurrentValue";
    var IdProses = "@subAktivitasNilaiARsesuaiCqdlpgEdit.idProses.CurrentValue";
    var idAktivitas = "@subAktivitasNilaiARsesuaiCqdlpgEdit.idAktifitas.CurrentValue";
    var NamaAktivitas = $('#el_SubAktivitasNilaiARsesuaiCQDLPG_idAktifitas').text();
    var parent_url = ew.PATH_BASE;
    const userLevel = "@CurrentUserLevel()";
    let currentStatusAktivitas = "";
    var steps = [
        { number: 1, label: "Proses Penerimaan", url: `${parent_url}PenimbunanList`, icon: "fa-oil-can" },
        { number: 2, label: "Proses", url: `${parent_url}ProsesList?cmd=search&x_IdReferensi%5B%5D=${IdReferensi}&search=&searchtype=`, icon: "fa-gears" },
        { number: 3, label: "Aktivitas", url: `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`, icon: "fa-list-check" },
        { number: 4, label: NamaAktivitas, url: '', icon: "fa-file-circle-check" }
    ];
    var currentStep = 4;
    $("<style>")
        .prop("type", "text/css")
        .html(`
            .stepper {
                display: flex;
                counter-reset: step;
                flex-wrap: nowrap;        /* jangan pecah ke bawah */
                overflow-x: auto;         /* scroll horizontal */
                -webkit-overflow-scrolling: touch; /* smooth scroll di mobile */
                padding-bottom: 8px;      /* space biar scroll bar gak nempel */
            }
            .stepper::-webkit-scrollbar {
            height: 6px; /* tipis */
            }
            .stepper::-webkit-scrollbar-thumb {
                background-color: #ccc;
                border-radius: 3px;
            }
            .step {
                width: 150px;
                text-align: center;
                position: relative;
            }
            .step:not(:last-child)::after {
                content: '';
                position: absolute;
                top: 20px;
                right: -50%;
                width: 100%;
                height: 4px;
                background-color: #ccc;
                z-index: 0;
            }
            .step.completed:not(:last-child)::after {
                background-color: #3c6dbd;
            }
            .step-icon {
                width: 36px;
                height: 36px;
                line-height: 36px;
                border-radius: 50%;
                background: #ccc;
                display: inline-block;
                z-index: 1;
                position: relative;
                color: white;
                font-weight: bold;
            }
            .step.active .step-icon {
                background: #3c6dbd;
            }
            .step-label {
                margin-top: 8px;
                font-size: 14px;
                display: flex;
                justify-content: center;
                align-items: center;
                gap: 6px;
                color: #333;
            }
            .step-label {
                color: #3c6dbd !important;
                text-decoration: none;
                margin: 0;
                padding: 0;
            }
            .step-label-not-active {
                color: #CCCCCC !important;
            }
            .ag-theme-quartz .ag-header {
                background-color: #3c6dbd !important;
            }
            .ag-theme-quartz .ag-header-cell-label {
                color: #ffffff !important;
            }
            .ag-theme-quartz .ag-icon-filter {
                color: #ffffff !important;
            }
            .rotate-down {
                transform: rotate(90deg);
                transition: transform 0.3s ease;
            }
            .rotate-right {
                transform: rotate(0deg);
                transition: transform 0.3s ease;
            }
            .header-table {
                background-color: #3c6dbd !important;
                color: #fff !important;
            }
        `)
        .appendTo("head");
    var $stepperWrapper = $('<div class="d-flex border-bottom border-2 pb-2"><div class="stepper"></div></div>');
    var $stepper = $stepperWrapper.find(".stepper");
    steps.forEach(function (step, i) {
        var isActive = step.number === currentStep;
        var isCompleted = step.number < currentStep;
        var stepClass = isActive ? "active" : isCompleted ? "completed" : "";
        var icon, labelContent, html;
        if (isActive) {
            icon = `<div class="step-icon bg-success">${step.number}</div>`;
            labelContent = `<div class="step-label"><p class="text-success mb-1"><i class="fa-solid ${step.icon} me-1 text-success"></i> ${step.label}</p></div>`;
            html = `<div class="step ${stepClass} text-success">${icon}${labelContent}</div>`;
        } else if (isCompleted) {
            icon = `<div class="step-icon" style="background: #3c6dbd;">${step.number}</div>`;
            labelContent = `<div class="step-label"><a href="${step.url}" class="text-primary"><i class="fa-solid ${step.icon} me-1"></i>${step.label}</a></div>`;
            html = `<div class="step ${stepClass}">${icon}${labelContent}</div>`;
        } else {
            icon = `<div class="step-icon">${step.number}</div>`;
            labelContent = `<div class="step-label-not-active"><p style="margin: 0;"><i class="fa-solid ${step.icon} me-1"></i>${step.label}</p></div>`;
            html = `<div class="step ${stepClass} text-secondary">${icon}${labelContent}</div>`;
        }
        $stepper.append(html);
    });
    $(".content-header .container-fluid .row").append($stepperWrapper);
    $("#btn-action").hide();
    $("#btn-cancel").html('<i class="fa-solid fa-ban"></i></i> Cancel');
    const $cancelButton = $("#btn-cancel");
    const idAktifitas = $("#x_idAktifitas").val();

    // --- log aktivitas start ---
    $('#el_SubAktivitasNilaiARsesuaiCQDLPG_LastUpdatedBy').addClass('d-flex gap-3');
    $('#el_SubAktivitasNilaiARsesuaiCQDLPG_LastUpdatedBy').append(`
        <button type="button" class="btn btn-primary ew-btn ps-2" expand-log="false">
            <i class="fa-solid fa-caret-right fa-2xl me-2 caret-icon rotate-right"></i>
            Log History
            <i class="fa-solid fa-clock-rotate-left ms-1"></i> 
        </button>
    `);
    $(document).on('click', 'button[expand-log]', function () {
        const $btn = $(this);
        const isExpanded = $btn.attr('expand-log') === 'true';
        const $row = $('#log-table-row');
        const $wrapper = $('#log-table-wrapper');
        const $caret = $btn.find('.caret-icon');
        if (!isExpanded) {
            $btn.attr('expand-log', 'true');
            $row.removeClass('d-none');
            setTimeout(() => {
                $wrapper.css({
                    maxHeight: '500px'
                });
            }, 10);
            $caret.removeClass('rotate-right').addClass('rotate-down');
        } else {
            $btn.attr('expand-log', 'false');
            $wrapper.css({
                maxHeight: '0'
            });
            setTimeout(() => {
                $row.addClass('d-none');
            }, 600);
            $caret.removeClass('rotate-down').addClass('rotate-right');
        }
    });
    if (idAktivitas && idAktivitas !== "0") {
        $.ajax({
            url: parent_url + `api/LookUpList/sub-aktivitas/startup?idAktivitas=${idAktivitas}`,
            type: 'GET',
            success: function(response) {
                // cek status
                if ($cancelButton.length){
                    let status = response.StatusAktivitas;
                    currentStatusAktivitas = status;
                    let $customButtons = ``;
                    switch (status) {
                        case "Waiting":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-draft">
                                    <i class="fas fa-save"></i> Save
                                </button>
                            `;
                            break;
                        case "Draft":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-draft">
                                    <i class="fas fa-save"></i> Save
                                </button>
                                <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completed">
                                    <i class="fas fa-paper-plane"></i> Submit
                                </button>
                            `
                            break;
                        case "Completed":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-editing">
                                    <i class="fa-solid fa-pen-to-square"></i> Edit
                                </button>
                                <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completedEdit">
                                    <i class="fas fa-paper-plane"></i> Submit
                                </button>
                            `;
                            break;
                        case "Editing":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-editing">
                                    <i class="fa-solid fa-pen-to-square"></i> Edit
                                </button>
                                <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completedEdit">
                                    <i class="fas fa-paper-plane"></i> Submit
                                </button>
                            `;
                            break;
                        case "Completed Edit":
                            if (userLevel === '4'){
                                $customButtons = `
                                    <button type="button" class="btn btn-warning ew-btn ml-2" id="btn-reverse-status">
                                        <i class="fa-solid fa-pen-to-square"></i> Edit 
                                    </button>
                                `;
                            }
                            break;
                    }
                    $cancelButton.before($customButtons);
                }

                // Cek is editable
                if(!response.IsEditable || response.StatusAktivitas === "Completed Edit"){
                    $("#btn-draft").hide();
                    $("#btn-editing").hide();
                    $("#btn-completed").hide();
                    $("#btn-completedEdit").hide();
                    $('input, select, textarea').prop('disabled', true);
                    $('#catatanReverseStatusEditing').prop('disabled', false);
                }

                // Log
                const mappedData = response.LogAktivitas.map(item => ({
                    Aktivitas: item.Aktivitas,
                    Username: item.UserName,
                    StatusAktivitas: item.StatusAktivitas,
                    Tanggal: item.Tanggal,
                    Catatan: item.Catatan
                }));
                $.getScript('https://cdn.jsdelivr.net/npm/ag-grid-community/dist/ag-grid-community.min.js', function () {
                        $('#r_LastUpdatedBy').after(`
                            <div id="log-table-row" class="d-none row m-0">
                                <div class="p-0 col-2"></div>
                                <div class="p-0 col-10">
                                    <div id="log-table-wrapper" style="overflow: hidden; transition: all 0.6s ease; max-height: 0;">
                                        <div id="myGrid" class="ag-theme-quartz w-100 my-3" style="height: 300px;"></div>
                                    </div>
                                </div>
                            </div>
                        `);
                        const gridOptions = {
                            rowData: mappedData,
                            columnDefs: [
                                {
                                    headerName: 'No.',
                                    valueGetter: 'node.rowIndex + 1',
                                    width: 80,
                                    filter: false
                                },
                                {
                                    headerName: 'Aktivitas',
                                    field: 'Aktivitas',
                                    flex: 1
                                },
                                {
                                    headerName: 'Username',
                                    field: 'Username',
                                    flex: 1
                                },
                                {
                                    headerName: 'Status Aktivitas',
                                    field: 'StatusAktivitas',
                                    flex: 1
                                },
                                {
                                    headerName: 'Tanggal',
                                    field: 'Tanggal',
                                    flex: 1
                                },
                                {
                                    headerName: 'Catatan',
                                    field: 'Catatan',
                                    flex: 1
                                }
                            ],
                            defaultColDef: {
                                sortable: false,
                                suppressMovable: true,
                                filter: true,
                                resizable: false
                            },
                            rowSelection: 'multiple',
                            suppressRowClickSelection: true,
                            pagination: true,
                            paginationPageSize: 10
                        };
                        const myGridElement = document.querySelector('#myGrid');
                        agGrid.createGrid(myGridElement, gridOptions);
                    });
            },
            error: function(xhr, status, error) {
                ew.alert(xhr.responseJSON.message ?? "Terjadi kesalahan dalam memuat data");
            }
        });
    } else {
        console.warn("Skipping startup API: Invalid IdAktivitas");
        $('button[expand-log]').hide(); 
        $("#btn-draft, #btn-editing, #btn-completed, #btn-completedEdit").hide();
        $('input, select, textarea').prop('disabled', true);
    }
    // --- log aktivitas end ---
     const tableHtml = `
               <div id="robSection">
                <div id="table_bl_sfad" class="table-responsive border rounded-3 col-12 col-md-9 mb-4">
                    <table class="table rounded-4 m-0">
                        <thead>
                            <tr>
                                <td class="fw-bold header-table">Satuan</th>
                                <td class="fw-bold header-table">BL</th>
                                <td class="fw-bold header-table">SFAL</th>
                                <td class="fw-bold header-table">SFBD</th>
                                <td class="fw-bold header-table">AR</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td><strong>Metric Ton</strong></td>
                                <td id="td_BL_Mt"></td>
                                <td id="td_SFAL_Mt"></td>
                                <td id="td_SFBD_Mt"></td>
                                <td id="td_AR_Mt"></td>
                            </tr>
                        </tbody>
                    </table>
                </div>           
            </div>
            <table aria-hidden="true" class="table table-hover view-page-table w-25 mb-3">
                <tbody>
                    <tr>
                        <td class="text-break fw-bold">R1</td>
                        <td class="text-break body-table fw-bold" id="R1"></td>
                    </tr>
                    <tr>
                        <td class="text-break fw-bold">R2</td>
                        <td class="text-break body-table fw-bold" id="R2"></td>
                    </tr>
                    <tr>
                        <td class="text-break fw-bold">R3</td>
                        <td class="text-break body-table fw-bold" id="R3"></td>
                    </tr>
                    <tr>
                        <td class="text-break fw-bold">R4</td>
                        <td class="text-break body-table fw-bold" id="R4"></td>
                    </tr>
                </tbody>
            </table>
                `;
            $("#r_lastUpdatedDate").after(tableHtml);
            const moveField = (fieldId, targetTdId) => {
                const fieldRow = $("#" + fieldId).closest(".row");
                $("#" + fieldId).appendTo("#" + targetTdId); // move field to table
                fieldRow.hide(); // hide original
            };
            moveField("el_SubAktivitasNilaiARsesuaiCQDLPG_MT", "td_AR_Mt");
    $.ajax({
            url: parent_url + `api/LookUpList/GetNomorPenerimaanFromPenimbunan?NomorPenimbunan=${IdReferensi}`,
            method: "GET",
            success: function (res) {
                if (res.success) {
                    const noReferensi = res.nomorPenerimaan;

                    // Panggil API SFBD
                    $.get(parent_url + `api/LookUpList/GetSFBDValue?NoReferensi=${noReferensi}&Jenis=LPG`, function (data) {
                        if (data.success) {
                            $("#td_SFBD_Mt").text(data.mt);
                        }
                    });

                    // Panggil API BLSFAL
                    $.get(parent_url + `api/LookUpList/GetBLSFALValue?NoReferensi=${noReferensi}&Jenis=LPG`, function (data) {
                        if (data.success) {
                            // BL
                            $("#td_BL_Mt").text(data.bl_mt);
                            // SFAL
                            $("#td_SFAL_Mt").text(data.sfal_mt);
                        }
                    });
                } else {
                    alert("Gagal mendapatkan nomor referensi: " + res.message);
                }
            }
        });
    $.ajax({
        url: parent_url + "api/LookUpList/GetRValue",
        type: "GET",
        data: {
            NoReferensi: IdReferensi,
            Menu: "AR",
            Jenis: "LPG"
        },
        success: function(rs){
            if (rs.success) {

                ["R1", "R2", "R3", "R4"].forEach(key => {
                    let val = parseFloat(rs[key]);
                    const el = document.getElementById(key);
                    if (!isNaN(val)) {
                        let color = "";
                        val = val * 100;
                        if (key === "R2") {
                            if (val < -0.07) {
                                color = "#ba313b";
                            } else if (val >= -0.07 && val <= 0) {
                                color = "#3c6dbd";
                            } else {
                                color = "#198754";
                            }
                        } else {
                            if (val < -0.125) {
                                color = "#ba313b";
                            } else if (val >= -0.125 && val <= 0) {
                                color = "#3c6dbd";
                            } else {
                                color = "#198754";
                            }
                        }
                        el.textContent = val.toFixed(3) + "%";
                        el.style.color = color;
                    }
                });
            }
        },
        error: function (xhr, status, error) {
            console.log("Error: " + xhr.responseText);
        }
    });    
    let uploadModal = `<div class="modal fade" id="warningValueModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="modal-title fs-5" id="warningValueModalTitle">Konfirmasi Simpan Data</h1>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body" id='warningValueText'>
            Konfirmasi 
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" id="btn-cancel-modal">Batal</button>
            <button type="button" class="btn btn-primary" id="btn-confirm-modal">Lanjutkan</button>
          </div>
        </div>
      </div>
    </div>`
    $("body").append(uploadModal);
    $("#btn-cancel-modal").click(function (event) {
        event.preventDefault();
        $("#warningValueModal").modal("hide");
    });
    $('#x_MT')
        .attr('inputmode', 'numeric')
        .on('input', function () {
            let val = $(this).val();
            let isNegative = val.startsWith('-');
            val = val.replace(',', '.');
            val = val.replace(/[^0-9.]/g, '');
            const parts = val.split('.');
            if (parts.length > 2) {
                val = parts[0] + '.' + parts.slice(1).join('');
            }
            if (isNegative) {
                val = '-' + val;
            }
            $(this).val(val);
        });
     let reverseStatusModal = `<div class="modal fade" id="confirmReverseStatusEditingModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
                  <div class="modal-dialog">
                    <div class="modal-content">
                      <div class="modal-header bg-primary">
                        <h1 class="modal-title fs-5" id="confirmReverseStatusEditingModalTitle">Konfirmasi Kembalikan Status</h1>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                      </div>
                      <form id="reverseStatusEditingModalForm">
                        <div class="modal-body px-2 py-1">
                            <div class="mb-3">
                                <label for="message-text" class="col-form-label">Catatan:</label>
                                <textarea class="form-control" id="catatanReverseStatusEditing" required rows="4"></textarea>
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" id="btnReverseStatusEditingModalCancel">Batal</button>
                            <button type="submit" class="btn btn-primary" id="btnReverseStatusEditingModalSubmit">Lanjutkan</button>
                        </div>
                      </form>
                    </div>
                  </div>
                </div>`
    $("body").append(reverseStatusModal);
    $("#btnReverseStatusEditingModalCancel").click(function (event) {
        event.preventDefault();
        $("#confirmReverseStatusEditingModal").modal("hide");
    });
    $(document).on("click", "#btn-reverse-status", function () {
        $("#confirmReverseStatusEditingModal").modal("show");
    });
});
</script>
