@{
    // View
    View = this;

    // Check action result
    if (setUserModDelete.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = setUserModDelete.UseLayout ? "_Layout" : "";
}
<script>
var currentTable = @Html.Raw(ConvertToJson(setUserModDelete.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { SetUserMOD: currentTable } });
var currentPageID = ew.PAGE_ID = "delete";
var currentForm;
var fSetUserMODdelete;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fSetUserMODdelete")
        .setPageId("delete")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@setUserModDelete.ShowPageHeader()
@setUserModDelete.ShowMessages()
<form name="fSetUserMODdelete" id="fSetUserMODdelete" class="ew-form ew-delete-form" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="delete">
@foreach (string key in setUserModDelete.RecordKeys) {
<input type="hidden" name="key_m[]" value="@key">
}
<div class="card ew-card ew-grid @(setUserModDelete.TableGridClass)">
<div class="card-body ew-grid-middle-panel @(setUserModDelete.TableContainerClass)" style="@(setUserModDelete.TableContainerStyle)">
<table class="@(setUserModDelete.TableClass)">
    <thead>
    <tr class="ew-table-header">
@if (setUserModDelete.Tanggal.Visible) { // setUserModDelete.Tanggal.Visible (begin)
        <th class="@Html.Raw(setUserModDelete.Tanggal.HeaderCellClass)"><span id="elh_SetUserMOD_Tanggal" class="SetUserMOD_Tanggal">@Html.Raw(setUserModDelete.Tanggal.Caption)</span></th>
} @* setUserModDelete.Tanggal.Visible (end) *@
@if (setUserModDelete.Plant.Visible) { // setUserModDelete.Plant.Visible (begin)
        <th class="@Html.Raw(setUserModDelete.Plant.HeaderCellClass)"><span id="elh_SetUserMOD_Plant" class="SetUserMOD_Plant">@Html.Raw(setUserModDelete.Plant.Caption)</span></th>
} @* setUserModDelete.Plant.Visible (end) *@
@if (setUserModDelete.UserMOD.Visible) { // setUserModDelete.UserMOD.Visible (begin)
        <th class="@Html.Raw(setUserModDelete.UserMOD.HeaderCellClass)"><span id="elh_SetUserMOD_UserMOD" class="SetUserMOD_UserMOD">@Html.Raw(setUserModDelete.UserMOD.Caption)</span></th>
} @* setUserModDelete.UserMOD.Visible (end) *@
@if (setUserModDelete.userInput.Visible) { // setUserModDelete.userInput.Visible (begin)
        <th class="@Html.Raw(setUserModDelete.userInput.HeaderCellClass)"><span id="elh_SetUserMOD_userInput" class="SetUserMOD_userInput">@Html.Raw(setUserModDelete.userInput.Caption)</span></th>
} @* setUserModDelete.userInput.Visible (end) *@
@if (setUserModDelete.etlDate.Visible) { // setUserModDelete.etlDate.Visible (begin)
        <th class="@Html.Raw(setUserModDelete.etlDate.HeaderCellClass)"><span id="elh_SetUserMOD_etlDate" class="SetUserMOD_etlDate">@Html.Raw(setUserModDelete.etlDate.Caption)</span></th>
} @* setUserModDelete.etlDate.Visible (end) *@
@if (setUserModDelete.LastUpdatedBy.Visible) { // setUserModDelete.LastUpdatedBy.Visible (begin)
        <th class="@Html.Raw(setUserModDelete.LastUpdatedBy.HeaderCellClass)"><span id="elh_SetUserMOD_LastUpdatedBy" class="SetUserMOD_LastUpdatedBy">@Html.Raw(setUserModDelete.LastUpdatedBy.Caption)</span></th>
} @* setUserModDelete.LastUpdatedBy.Visible (end) *@
@if (setUserModDelete.lastUpdatedDate.Visible) { // setUserModDelete.lastUpdatedDate.Visible (begin)
        <th class="@Html.Raw(setUserModDelete.lastUpdatedDate.HeaderCellClass)"><span id="elh_SetUserMOD_lastUpdatedDate" class="SetUserMOD_lastUpdatedDate">@Html.Raw(setUserModDelete.lastUpdatedDate.Caption)</span></th>
} @* setUserModDelete.lastUpdatedDate.Visible (end) *@
    </tr>
    </thead>
    <tbody>
@{ setUserModDelete.RecordCount = 0; }
@while (setUserModDelete.Recordset != null && await setUserModDelete.Recordset.ReadAsync()) { // DN
    setUserModDelete.RecordCount++;
    setUserModDelete.RowCount++;

    // Set row properties
    setUserModDelete.ResetAttributes();
    setUserModDelete.RowType = RowType.View; // View

    // Get the field contents
    await setUserModDelete.LoadRowValues(setUserModDelete.Recordset);

    // Render row
    await setUserModDelete.RenderRow();
    <tr @Html.Raw(setUserModDelete.RowAttributes)>
@if (setUserModDelete.Tanggal.Visible) { // setUserModDelete.Tanggal.Visible (begin)
        <td @Html.Raw(setUserModDelete.Tanggal.CellAttributes)>
<span id="">
<span @Html.Raw(setUserModDelete.Tanggal.ViewAttributes)>
@Html.Raw(setUserModDelete.Tanggal.GetViewValue())</span>
</span></td>
} @* setUserModDelete.Tanggal.Visible (end) *@
@if (setUserModDelete.Plant.Visible) { // setUserModDelete.Plant.Visible (begin)
        <td @Html.Raw(setUserModDelete.Plant.CellAttributes)>
<span id="">
<span @Html.Raw(setUserModDelete.Plant.ViewAttributes)>
@Html.Raw(setUserModDelete.Plant.GetViewValue())</span>
</span></td>
} @* setUserModDelete.Plant.Visible (end) *@
@if (setUserModDelete.UserMOD.Visible) { // setUserModDelete.UserMOD.Visible (begin)
        <td @Html.Raw(setUserModDelete.UserMOD.CellAttributes)>
<span id="">
<span @Html.Raw(setUserModDelete.UserMOD.ViewAttributes)>
@Html.Raw(setUserModDelete.UserMOD.GetViewValue())</span>
</span></td>
} @* setUserModDelete.UserMOD.Visible (end) *@
@if (setUserModDelete.userInput.Visible) { // setUserModDelete.userInput.Visible (begin)
        <td @Html.Raw(setUserModDelete.userInput.CellAttributes)>
<span id="">
<span @Html.Raw(setUserModDelete.userInput.ViewAttributes)>
@Html.Raw(setUserModDelete.userInput.GetViewValue())</span>
</span></td>
} @* setUserModDelete.userInput.Visible (end) *@
@if (setUserModDelete.etlDate.Visible) { // setUserModDelete.etlDate.Visible (begin)
        <td @Html.Raw(setUserModDelete.etlDate.CellAttributes)>
<span id="">
<span @Html.Raw(setUserModDelete.etlDate.ViewAttributes)>
@Html.Raw(setUserModDelete.etlDate.GetViewValue())</span>
</span></td>
} @* setUserModDelete.etlDate.Visible (end) *@
@if (setUserModDelete.LastUpdatedBy.Visible) { // setUserModDelete.LastUpdatedBy.Visible (begin)
        <td @Html.Raw(setUserModDelete.LastUpdatedBy.CellAttributes)>
<span id="">
<span @Html.Raw(setUserModDelete.LastUpdatedBy.ViewAttributes)>
@Html.Raw(setUserModDelete.LastUpdatedBy.GetViewValue())</span>
</span></td>
} @* setUserModDelete.LastUpdatedBy.Visible (end) *@
@if (setUserModDelete.lastUpdatedDate.Visible) { // setUserModDelete.lastUpdatedDate.Visible (begin)
        <td @Html.Raw(setUserModDelete.lastUpdatedDate.CellAttributes)>
<span id="">
<span @Html.Raw(setUserModDelete.lastUpdatedDate.ViewAttributes)>
@Html.Raw(setUserModDelete.lastUpdatedDate.GetViewValue())</span>
</span></td>
} @* setUserModDelete.lastUpdatedDate.Visible (end) *@
    </tr>
}
@{ setUserModDelete.CloseRecordset(); }
</tbody>
</table>
</div>
</div>
<div class="ew-buttons ew-desktop-buttons">
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("DeleteBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(setUserModDelete.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
</div>
</form>
@setUserModDelete.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
loadjs.ready("load", function() {
    // Startup script
    // Write your table-specific startup script here
    // document.write("page loaded");
    (function () {
        const role = parseInt('@CurrentUserInfo("Rule")') || 0; // 0=Superadmin, 10=OH
        const isAllowed = (role === 0 || role === 10);
        console.log("Role = ", role)
        if (isAllowed) return;

        // ====== Blokir akses & tampilkan peringatan ======
        const $container = $(".content .container-fluid").first();
        const $form = $("form.ew-form").first();
        const listUrl = ew.PATH_BASE + "SetUserMODList";

        // Alert peringatan (Bootstrap)
        const $alert = $(`
          <div class="alert alert-warning d-flex align-items-start" role="alert" id="no-access-alert">
            <i class="fa-solid fa-triangle-exclamation me-2 mt-1"></i>
            <div>
              <div class="fw-bold">Tidak ada akses untuk menghapus Set User MOD.</div>
              <div class="small">
                Hanya pengguna dengan peran <strong>Superadmin</strong> (Rule 0) atau <strong>OH</strong> (Rule 10) yang dapat menghapus data di halaman ini.
                <br>Solusi: <em>Ganti Akun</em> atau <em>minta diberikan akses oleh OH</em>.
              </div>
              <div class="mt-2">
                <a href="${ew.PATH_BASE}logout" class="btn btn-outline-secondary btn-sm me-2">
                  <i class="fa-solid fa-right-from-bracket"></i> Ganti Akun
                </a>
                <a href="${listUrl}" class="btn btn-primary btn-sm">
                  <i class="fa-solid fa-list"></i> Kembali ke List
                </a>
              </div>
            </div>
          </div>
        `);

        // Sisipkan alert di atas form
        if ($container.length) $container.prepend($alert);
        else $("body").prepend($alert);

        // Nonaktifkan input & tombol di form
        if ($form.length) {
          // Blokir submit (klik, enter, dll.)
          $form.on("submit", function (e) { e.preventDefault(); return false; });

          // Disable semua kontrol
          $form.find("input, select, textarea, button").prop("disabled", true);

          // Re-enable tombol/link Cancel jika ada
          $form.find('a.btn-secondary, a.ew-btn, a.btn')
               .filter(function () {
                  const href = (this.getAttribute("href") || "").toLowerCase();
                  return href.includes("list") || href.includes("cancel");
               })
               .prop("disabled", false)
               .removeClass("disabled");
        }

        // Bersihkan tombol Add/Save lain yang mungkin muncul
        $(".ew-toolbar a.ew-add, .ew-grid-lower-panel a, button[name='btn-action'], button[name='btn-submit'], [data-ew-action='delete'], .btn-danger").hide();

        // Tambahan UX: scroll ke atas supaya alert terlihat
        window.scrollTo({ top: 0, behavior: "smooth" });
      })();
});
</script>
