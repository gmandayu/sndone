@{
    // View
    View = this;

    // Check action result
    if (templateAktivitasDokumenDelete.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = templateAktivitasDokumenDelete.UseLayout ? "_Layout" : "";
}
<script>
var currentTable = @Html.Raw(ConvertToJson(templateAktivitasDokumenDelete.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { TemplateAktivitasDokumen: currentTable } });
var currentPageID = ew.PAGE_ID = "delete";
var currentForm;
var fTemplateAktivitasDokumendelete;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fTemplateAktivitasDokumendelete")
        .setPageId("delete")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Client script
    $('#fTemplateAktivitasDokumendelete').on('submit', function (e) {
        e.preventDefault();
        showLoading();
        setTimeout(() => {
            hideLoading();
        }, 3000);
        this.requestSubmit();
    });
});
</script>
@templateAktivitasDokumenDelete.ShowPageHeader()
@templateAktivitasDokumenDelete.ShowMessages()
<form name="fTemplateAktivitasDokumendelete" id="fTemplateAktivitasDokumendelete" class="ew-form ew-delete-form" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="delete">
@foreach (string key in templateAktivitasDokumenDelete.RecordKeys) {
<input type="hidden" name="key_m[]" value="@key">
}
<div class="card ew-card ew-grid @(templateAktivitasDokumenDelete.TableGridClass)">
<div class="card-body ew-grid-middle-panel @(templateAktivitasDokumenDelete.TableContainerClass)" style="@(templateAktivitasDokumenDelete.TableContainerStyle)">
<table class="@(templateAktivitasDokumenDelete.TableClass)">
    <thead>
    <tr class="ew-table-header">
@if (templateAktivitasDokumenDelete.IdTemplateAktivitas.Visible) { // templateAktivitasDokumenDelete.IdTemplateAktivitas.Visible (begin)
        <th class="@Html.Raw(templateAktivitasDokumenDelete.IdTemplateAktivitas.HeaderCellClass)"><span id="elh_TemplateAktivitasDokumen_IdTemplateAktivitas" class="TemplateAktivitasDokumen_IdTemplateAktivitas">@Html.Raw(templateAktivitasDokumenDelete.IdTemplateAktivitas.Caption)</span></th>
} @* templateAktivitasDokumenDelete.IdTemplateAktivitas.Visible (end) *@
@if (templateAktivitasDokumenDelete.IdDokumen.Visible) { // templateAktivitasDokumenDelete.IdDokumen.Visible (begin)
        <th class="@Html.Raw(templateAktivitasDokumenDelete.IdDokumen.HeaderCellClass)"><span id="elh_TemplateAktivitasDokumen_IdDokumen" class="TemplateAktivitasDokumen_IdDokumen">@Html.Raw(templateAktivitasDokumenDelete.IdDokumen.Caption)</span></th>
} @* templateAktivitasDokumenDelete.IdDokumen.Visible (end) *@
@if (templateAktivitasDokumenDelete.WajibUpload.Visible) { // templateAktivitasDokumenDelete.WajibUpload.Visible (begin)
        <th class="@Html.Raw(templateAktivitasDokumenDelete.WajibUpload.HeaderCellClass)"><span id="elh_TemplateAktivitasDokumen_WajibUpload" class="TemplateAktivitasDokumen_WajibUpload">@Html.Raw(templateAktivitasDokumenDelete.WajibUpload.Caption)</span></th>
} @* templateAktivitasDokumenDelete.WajibUpload.Visible (end) *@
    </tr>
    </thead>
    <tbody>
@{ templateAktivitasDokumenDelete.RecordCount = 0; }
@while (templateAktivitasDokumenDelete.Recordset != null && await templateAktivitasDokumenDelete.Recordset.ReadAsync()) { // DN
    templateAktivitasDokumenDelete.RecordCount++;
    templateAktivitasDokumenDelete.RowCount++;

    // Set row properties
    templateAktivitasDokumenDelete.ResetAttributes();
    templateAktivitasDokumenDelete.RowType = RowType.View; // View

    // Get the field contents
    await templateAktivitasDokumenDelete.LoadRowValues(templateAktivitasDokumenDelete.Recordset);

    // Render row
    await templateAktivitasDokumenDelete.RenderRow();
    <tr @Html.Raw(templateAktivitasDokumenDelete.RowAttributes)>
@if (templateAktivitasDokumenDelete.IdTemplateAktivitas.Visible) { // templateAktivitasDokumenDelete.IdTemplateAktivitas.Visible (begin)
        <td @Html.Raw(templateAktivitasDokumenDelete.IdTemplateAktivitas.CellAttributes)>
<span id="">
<span @Html.Raw(templateAktivitasDokumenDelete.IdTemplateAktivitas.ViewAttributes)>
@Html.Raw(templateAktivitasDokumenDelete.IdTemplateAktivitas.GetViewValue())</span>
</span></td>
} @* templateAktivitasDokumenDelete.IdTemplateAktivitas.Visible (end) *@
@if (templateAktivitasDokumenDelete.IdDokumen.Visible) { // templateAktivitasDokumenDelete.IdDokumen.Visible (begin)
        <td @Html.Raw(templateAktivitasDokumenDelete.IdDokumen.CellAttributes)>
<span id="">
<span @Html.Raw(templateAktivitasDokumenDelete.IdDokumen.ViewAttributes)>
@Html.Raw(templateAktivitasDokumenDelete.IdDokumen.GetViewValue())</span>
</span></td>
} @* templateAktivitasDokumenDelete.IdDokumen.Visible (end) *@
@if (templateAktivitasDokumenDelete.WajibUpload.Visible) { // templateAktivitasDokumenDelete.WajibUpload.Visible (begin)
        <td @Html.Raw(templateAktivitasDokumenDelete.WajibUpload.CellAttributes)>
<span id="">
<span @Html.Raw(templateAktivitasDokumenDelete.WajibUpload.ViewAttributes)>
<i class="fa-regular @(ConvertToBool(templateAktivitasDokumenDelete.WajibUpload.CurrentValue) ? "fa-square-check" : "fa-square") ew-icon ew-boolean"></i>
</span>
</span></td>
} @* templateAktivitasDokumenDelete.WajibUpload.Visible (end) *@
    </tr>
}
@{ templateAktivitasDokumenDelete.CloseRecordset(); }
</tbody>
</table>
</div>
</div>
<div class="ew-buttons ew-desktop-buttons">
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("DeleteBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(templateAktivitasDokumenDelete.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
</div>
</form>
@templateAktivitasDokumenDelete.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
