@{
    // View
    View = this;

    // Check action result
    if (masterFungsiKunjunganDelete.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = masterFungsiKunjunganDelete.UseLayout ? "_Layout" : "";
}
<script>
var currentTable = @Html.Raw(ConvertToJson(masterFungsiKunjunganDelete.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { MasterFungsiKunjungan: currentTable } });
var currentPageID = ew.PAGE_ID = "delete";
var currentForm;
var fMasterFungsiKunjungandelete;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fMasterFungsiKunjungandelete")
        .setPageId("delete")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@masterFungsiKunjunganDelete.ShowPageHeader()
@masterFungsiKunjunganDelete.ShowMessages()
<form name="fMasterFungsiKunjungandelete" id="fMasterFungsiKunjungandelete" class="ew-form ew-delete-form" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="delete">
@foreach (string key in masterFungsiKunjunganDelete.RecordKeys) {
<input type="hidden" name="key_m[]" value="@key">
}
<div class="card ew-card ew-grid @(masterFungsiKunjunganDelete.TableGridClass)">
<div class="card-body ew-grid-middle-panel @(masterFungsiKunjunganDelete.TableContainerClass)" style="@(masterFungsiKunjunganDelete.TableContainerStyle)">
<table class="@(masterFungsiKunjunganDelete.TableClass)">
    <thead>
    <tr class="ew-table-header">
@if (masterFungsiKunjunganDelete.ID.Visible) { // masterFungsiKunjunganDelete.ID.Visible (begin)
        <th class="@Html.Raw(masterFungsiKunjunganDelete.ID.HeaderCellClass)"><span id="elh_MasterFungsiKunjungan_ID" class="MasterFungsiKunjungan_ID">@Html.Raw(masterFungsiKunjunganDelete.ID.Caption)</span></th>
} @* masterFungsiKunjunganDelete.ID.Visible (end) *@
@if (masterFungsiKunjunganDelete.FungsiKunjungan.Visible) { // masterFungsiKunjunganDelete.FungsiKunjungan.Visible (begin)
        <th class="@Html.Raw(masterFungsiKunjunganDelete.FungsiKunjungan.HeaderCellClass)"><span id="elh_MasterFungsiKunjungan_FungsiKunjungan" class="MasterFungsiKunjungan_FungsiKunjungan">@Html.Raw(masterFungsiKunjunganDelete.FungsiKunjungan.Caption)</span></th>
} @* masterFungsiKunjunganDelete.FungsiKunjungan.Visible (end) *@
    </tr>
    </thead>
    <tbody>
@{ masterFungsiKunjunganDelete.RecordCount = 0; }
@while (masterFungsiKunjunganDelete.Recordset != null && await masterFungsiKunjunganDelete.Recordset.ReadAsync()) { // DN
    masterFungsiKunjunganDelete.RecordCount++;
    masterFungsiKunjunganDelete.RowCount++;

    // Set row properties
    masterFungsiKunjunganDelete.ResetAttributes();
    masterFungsiKunjunganDelete.RowType = RowType.View; // View

    // Get the field contents
    await masterFungsiKunjunganDelete.LoadRowValues(masterFungsiKunjunganDelete.Recordset);

    // Render row
    await masterFungsiKunjunganDelete.RenderRow();
    <tr @Html.Raw(masterFungsiKunjunganDelete.RowAttributes)>
@if (masterFungsiKunjunganDelete.ID.Visible) { // masterFungsiKunjunganDelete.ID.Visible (begin)
        <td @Html.Raw(masterFungsiKunjunganDelete.ID.CellAttributes)>
<span id="">
<span @Html.Raw(masterFungsiKunjunganDelete.ID.ViewAttributes)>
@Html.Raw(masterFungsiKunjunganDelete.ID.GetViewValue())</span>
</span></td>
} @* masterFungsiKunjunganDelete.ID.Visible (end) *@
@if (masterFungsiKunjunganDelete.FungsiKunjungan.Visible) { // masterFungsiKunjunganDelete.FungsiKunjungan.Visible (begin)
        <td @Html.Raw(masterFungsiKunjunganDelete.FungsiKunjungan.CellAttributes)>
<span id="">
<span @Html.Raw(masterFungsiKunjunganDelete.FungsiKunjungan.ViewAttributes)>
@Html.Raw(masterFungsiKunjunganDelete.FungsiKunjungan.GetViewValue())</span>
</span></td>
} @* masterFungsiKunjunganDelete.FungsiKunjungan.Visible (end) *@
    </tr>
}
@{ masterFungsiKunjunganDelete.CloseRecordset(); }
</tbody>
</table>
</div>
</div>
<div class="ew-buttons ew-desktop-buttons">
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("DeleteBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(masterFungsiKunjunganDelete.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
</div>
</form>
@masterFungsiKunjunganDelete.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
