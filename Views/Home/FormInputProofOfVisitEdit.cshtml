@{
    // View
    View = this;

    // Check action result
    if (formInputProofOfVisitEdit.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = formInputProofOfVisitEdit.UseLayout ? "_Layout" : "";
}
@formInputProofOfVisitEdit.ShowPageHeader()
@formInputProofOfVisitEdit.ShowMessages()
<main class="edit">
<form name="fFormInputProofOfVisitedit" id="fFormInputProofOfVisitedit" class="@(formInputProofOfVisitEdit.FormClassName)" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
<script>
var currentTable = @Html.Raw(ConvertToJson(formInputProofOfVisitEdit.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { FormInputProofOfVisit: currentTable } });
var currentPageID = ew.PAGE_ID = "edit";
var currentForm;
var fFormInputProofOfVisitedit;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fFormInputProofOfVisitedit")
        .setPageId("edit")

        // Add fields
        .setFields([

            ["id", [fields.id.visible && fields.id.required ? ew.Validators.required(fields.id.caption) : null], fields.id.isInvalid],
            ["NoReferensi", [fields.NoReferensi.visible && fields.NoReferensi.required ? ew.Validators.required(fields.NoReferensi.caption) : null], fields.NoReferensi.isInvalid],
            ["Tanggal", [fields.Tanggal.visible && fields.Tanggal.required ? ew.Validators.required(fields.Tanggal.caption) : null, ew.Validators.datetime(fields.Tanggal.clientFormatPattern)], fields.Tanggal.isInvalid],
            ["Lokasi", [fields.Lokasi.visible && fields.Lokasi.required ? ew.Validators.required(fields.Lokasi.caption) : null], fields.Lokasi.isInvalid],
            ["Ketidaksesuaiaan", [fields.Ketidaksesuaiaan.visible && fields.Ketidaksesuaiaan.required ? ew.Validators.required(fields.Ketidaksesuaiaan.caption) : null], fields.Ketidaksesuaiaan.isInvalid],
            ["Keterangan", [fields.Keterangan.visible && fields.Keterangan.required ? ew.Validators.required(fields.Keterangan.caption) : null], fields.Keterangan.isInvalid],
            ["IdPosition", [fields.IdPosition.visible && fields.IdPosition.required ? ew.Validators.required(fields.IdPosition.caption) : null], fields.IdPosition.isInvalid],
            ["userInput", [fields.userInput.visible && fields.userInput.required ? ew.Validators.required(fields.userInput.caption) : null], fields.userInput.isInvalid],
            ["etlDate", [fields.etlDate.visible && fields.etlDate.required ? ew.Validators.required(fields.etlDate.caption) : null], fields.etlDate.isInvalid],
            ["lastUpdatedBy", [fields.lastUpdatedBy.visible && fields.lastUpdatedBy.required ? ew.Validators.required(fields.lastUpdatedBy.caption) : null], fields.lastUpdatedBy.isInvalid],
            ["lastUpdatedDate", [fields.lastUpdatedDate.visible && fields.lastUpdatedDate.required ? ew.Validators.required(fields.lastUpdatedDate.caption) : null], fields.lastUpdatedDate.isInvalid]
        ])

        // Form_CustomValidate
        .setCustomValidate(
            function(fobj) { // DO NOT CHANGE THIS LINE!
                    // Your custom validation code here, return false if invalid.
                    return true;
                }
        )

        // Use JavaScript validation or not
        .setValidateRequired(ew.CLIENT_VALIDATE)

        // Dynamic selection lists
        .setLists({
            "Lokasi": @Html.Raw(formInputProofOfVisitEdit.Lokasi.ToClientList(formInputProofOfVisitEdit)),
            "Ketidaksesuaiaan": @Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.ToClientList(formInputProofOfVisitEdit)),
        })
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="update">
<input type="hidden" name="modal" value="@ConvertToInt(formInputProofOfVisitEdit.IsModal)">
@if (IsJsonResponse()) {
<input type="hidden" name="json" value="1">
}
<input type="hidden" name="@(formInputProofOfVisitEdit.OldKeyName)" value="@Html.Raw(formInputProofOfVisitEdit.OldKey)">
<div class="ew-edit-div d-none"><!-- page* -->
@if (formInputProofOfVisitEdit.id.Visible) { // formInputProofOfVisitEdit.id.Visible (begin)
    <div id="r_id" @Html.Raw(formInputProofOfVisitEdit.id.RowAttributes)>
        <label id="elh_FormInputProofOfVisit_id" class="@(formInputProofOfVisitEdit.LeftColumnClass)"><template id="tpc_FormInputProofOfVisit_id">@Html.Raw(formInputProofOfVisitEdit.id.Caption)@Html.Raw(formInputProofOfVisitEdit.id.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(formInputProofOfVisitEdit.RightColumnClass)"><div @Html.Raw(formInputProofOfVisitEdit.id.CellAttributes)>
<template id="tpx_FormInputProofOfVisit_id"><span id="el_FormInputProofOfVisit_id">
<span @Html.Raw(formInputProofOfVisitEdit.id.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(formInputProofOfVisitEdit.id.EditValue)))"></span>
<input type="hidden" data-table="FormInputProofOfVisit" data-field="x_id" data-hidden="1" name="x_id" id="x_id" value="@Html.Raw(HtmlEncode(formInputProofOfVisitEdit.id.CurrentValue))">
</span></template>
</div></div>
    </div>
} @* formInputProofOfVisitEdit.id.Visible (end) *@
@if (formInputProofOfVisitEdit.NoReferensi.Visible) { // formInputProofOfVisitEdit.NoReferensi.Visible (begin)
    <div id="r_NoReferensi" @Html.Raw(formInputProofOfVisitEdit.NoReferensi.RowAttributes)>
        <label id="elh_FormInputProofOfVisit_NoReferensi" for="x_NoReferensi" class="@(formInputProofOfVisitEdit.LeftColumnClass)"><template id="tpc_FormInputProofOfVisit_NoReferensi">@Html.Raw(formInputProofOfVisitEdit.NoReferensi.Caption)@Html.Raw(formInputProofOfVisitEdit.NoReferensi.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(formInputProofOfVisitEdit.RightColumnClass)"><div @Html.Raw(formInputProofOfVisitEdit.NoReferensi.CellAttributes)>
<template id="tpx_FormInputProofOfVisit_NoReferensi"><span id="el_FormInputProofOfVisit_NoReferensi">
<input type="@(formInputProofOfVisitEdit.NoReferensi.InputTextType)" name="x_NoReferensi" id="x_NoReferensi" data-table="FormInputProofOfVisit" data-field="x_NoReferensi" value="@Html.Raw(formInputProofOfVisitEdit.NoReferensi.EditValue)" size="30" maxlength="255" placeholder="@Html.Raw(HtmlEncode(formInputProofOfVisitEdit.NoReferensi.PlaceHolder))" data-format-pattern="@(formInputProofOfVisitEdit.NoReferensi.FormatPattern)"@Html.Raw(formInputProofOfVisitEdit.NoReferensi.EditAttributes) aria-describedby="x_NoReferensi_help">
@Html.Raw(formInputProofOfVisitEdit.NoReferensi.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(formInputProofOfVisitEdit.NoReferensi.GetErrorMessage())</div>
</span></template>
</div></div>
    </div>
} @* formInputProofOfVisitEdit.NoReferensi.Visible (end) *@
@if (formInputProofOfVisitEdit.Tanggal.Visible) { // formInputProofOfVisitEdit.Tanggal.Visible (begin)
    <div id="r_Tanggal" @Html.Raw(formInputProofOfVisitEdit.Tanggal.RowAttributes)>
        <label id="elh_FormInputProofOfVisit_Tanggal" for="x_Tanggal" class="@(formInputProofOfVisitEdit.LeftColumnClass)"><template id="tpc_FormInputProofOfVisit_Tanggal">@Html.Raw(formInputProofOfVisitEdit.Tanggal.Caption)@Html.Raw(formInputProofOfVisitEdit.Tanggal.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(formInputProofOfVisitEdit.RightColumnClass)"><div @Html.Raw(formInputProofOfVisitEdit.Tanggal.CellAttributes)>
<template id="tpx_FormInputProofOfVisit_Tanggal"><span id="el_FormInputProofOfVisit_Tanggal">
<input type="@(formInputProofOfVisitEdit.Tanggal.InputTextType)" name="x_Tanggal" id="x_Tanggal" data-table="FormInputProofOfVisit" data-field="x_Tanggal" value="@Html.Raw(formInputProofOfVisitEdit.Tanggal.EditValue)" placeholder="@Html.Raw(HtmlEncode(formInputProofOfVisitEdit.Tanggal.PlaceHolder))" data-format-pattern="@(formInputProofOfVisitEdit.Tanggal.FormatPattern)"@Html.Raw(formInputProofOfVisitEdit.Tanggal.EditAttributes) aria-describedby="x_Tanggal_help">
@Html.Raw(formInputProofOfVisitEdit.Tanggal.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(formInputProofOfVisitEdit.Tanggal.GetErrorMessage())</div>
@if (!formInputProofOfVisitEdit.Tanggal.ReadOnly && !formInputProofOfVisitEdit.Tanggal.Disabled && !formInputProofOfVisitEdit.Tanggal.EditAttrs.Is("readonly") && !formInputProofOfVisitEdit.Tanggal.EditAttrs.Is("disabled")) {
<script>
loadjs.ready(["fFormInputProofOfVisitedit", "datetimepicker"], function () {
    let format = "@Html.Raw(DateFormat(109))", // DN
        options = {
                localization: {
                    locale: ew.LANGUAGE_ID + "-u-nu-" + ew.getNumberingSystem(),
                    hourCycle: format.match(/H/) ? "h24" : "h12",
                    format,
                    ...ew.language.phrase("datetimepicker")
                },
                display: {
                    icons: {
                        previous: ew.IS_RTL ? "fa-solid fa-chevron-right" : "fa-solid fa-chevron-left",
                        next: ew.IS_RTL ? "fa-solid fa-chevron-left" : "fa-solid fa-chevron-right"
                    },
                    components: {
                        clock: !!format.match(/h/i) || !!format.match(/m/) || !!format.match(/s/i),
                        hours: !!format.match(/h/i),
                        minutes: !!format.match(/m/),
                        seconds: !!format.match(/s/i)
                    },
                theme: ew.getPreferredTheme()
                }
            };
    ew.createDateTimePicker("fFormInputProofOfVisitedit", "x_Tanggal", ew.deepAssign({"useCurrent":false,"display":{"sideBySide":false}}, options));
});
</script>
}
</span></template>
</div></div>
    </div>
} @* formInputProofOfVisitEdit.Tanggal.Visible (end) *@
@if (formInputProofOfVisitEdit.Lokasi.Visible) { // formInputProofOfVisitEdit.Lokasi.Visible (begin)
    <div id="r_Lokasi" @Html.Raw(formInputProofOfVisitEdit.Lokasi.RowAttributes)>
        <label id="elh_FormInputProofOfVisit_Lokasi" for="x_Lokasi" class="@(formInputProofOfVisitEdit.LeftColumnClass)"><template id="tpc_FormInputProofOfVisit_Lokasi">@Html.Raw(formInputProofOfVisitEdit.Lokasi.Caption)@Html.Raw(formInputProofOfVisitEdit.Lokasi.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(formInputProofOfVisitEdit.RightColumnClass)"><div @Html.Raw(formInputProofOfVisitEdit.Lokasi.CellAttributes)>
<template id="tpx_FormInputProofOfVisit_Lokasi"><span id="el_FormInputProofOfVisit_Lokasi">
    <select
        id="x_Lokasi"
        name="x_Lokasi"
        class="form-select ew-select@(formInputProofOfVisitEdit.Lokasi.IsInvalidClass)"
        data-select2-id="fFormInputProofOfVisitedit_x_Lokasi"
        data-table="FormInputProofOfVisit"
        data-field="x_Lokasi"
        data-value-separator="@(formInputProofOfVisitEdit.Lokasi.DisplayValueSeparatorAttribute)"
        data-placeholder="@(HtmlEncode(formInputProofOfVisitEdit.Lokasi.PlaceHolder))"
        @Html.Raw(formInputProofOfVisitEdit.Lokasi.EditAttributes)>
        @(formInputProofOfVisitEdit.Lokasi.SelectOptionListHtml("x_Lokasi"))
    </select>
    @Html.Raw(formInputProofOfVisitEdit.Lokasi.GetCustomMessage)
    <div class="invalid-feedback">@Html.Raw(formInputProofOfVisitEdit.Lokasi.GetErrorMessage())</div>
@Html.Raw(formInputProofOfVisitEdit.Lokasi.Lookup.GetParamTag(formInputProofOfVisitEdit, "p_x_Lokasi"))
<script>
loadjs.ready("fFormInputProofOfVisitedit", function() {
    var options = { name: "x_Lokasi", selectId: "fFormInputProofOfVisitedit_x_Lokasi" },
        el = document.querySelector("select[data-select2-id='" + options.selectId + "']");
    if (!el)
        return;
    options.closeOnSelect = !options.multiple;
    options.dropdownParent = el.closest("#ew-modal-dialog, #ew-add-opt-dialog");
    if (fFormInputProofOfVisitedit.lists.Lokasi?.lookupOptions.length) {
        options.data = { id: "x_Lokasi", form: "fFormInputProofOfVisitedit" };
    } else {
        options.ajax = { id: "x_Lokasi", form: "fFormInputProofOfVisitedit", limit: ew.LOOKUP_PAGE_SIZE };
    }
    options.minimumResultsForSearch = Infinity;
    options = Object.assign({}, ew.selectOptions, options, ew.vars.tables.FormInputProofOfVisit.fields.Lokasi.selectOptions);
    ew.createSelect(options);
});
</script>
</span></template>
</div></div>
    </div>
} @* formInputProofOfVisitEdit.Lokasi.Visible (end) *@
@if (formInputProofOfVisitEdit.Ketidaksesuaiaan.Visible) { // formInputProofOfVisitEdit.Ketidaksesuaiaan.Visible (begin)
    <div id="r_Ketidaksesuaiaan" @Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.RowAttributes)>
        <label id="elh_FormInputProofOfVisit_Ketidaksesuaiaan" for="x_Ketidaksesuaiaan" class="@(formInputProofOfVisitEdit.LeftColumnClass)"><template id="tpc_FormInputProofOfVisit_Ketidaksesuaiaan">@Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.Caption)@Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(formInputProofOfVisitEdit.RightColumnClass)"><div @Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.CellAttributes)>
<template id="tpx_FormInputProofOfVisit_Ketidaksesuaiaan"><span id="el_FormInputProofOfVisit_Ketidaksesuaiaan">
    <select
        id="x_Ketidaksesuaiaan"
        name="x_Ketidaksesuaiaan"
        class="form-select ew-select@(formInputProofOfVisitEdit.Ketidaksesuaiaan.IsInvalidClass)"
        data-select2-id="fFormInputProofOfVisitedit_x_Ketidaksesuaiaan"
        data-table="FormInputProofOfVisit"
        data-field="x_Ketidaksesuaiaan"
        data-value-separator="@(formInputProofOfVisitEdit.Ketidaksesuaiaan.DisplayValueSeparatorAttribute)"
        data-placeholder="@(HtmlEncode(formInputProofOfVisitEdit.Ketidaksesuaiaan.PlaceHolder))"
        @Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.EditAttributes)>
        @(formInputProofOfVisitEdit.Ketidaksesuaiaan.SelectOptionListHtml("x_Ketidaksesuaiaan"))
    </select>
    @Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.GetCustomMessage)
    <div class="invalid-feedback">@Html.Raw(formInputProofOfVisitEdit.Ketidaksesuaiaan.GetErrorMessage())</div>
<script>
loadjs.ready("fFormInputProofOfVisitedit", function() {
    var options = { name: "x_Ketidaksesuaiaan", selectId: "fFormInputProofOfVisitedit_x_Ketidaksesuaiaan" },
        el = document.querySelector("select[data-select2-id='" + options.selectId + "']");
    if (!el)
        return;
    options.closeOnSelect = !options.multiple;
    options.dropdownParent = el.closest("#ew-modal-dialog, #ew-add-opt-dialog");
    if (fFormInputProofOfVisitedit.lists.Ketidaksesuaiaan?.lookupOptions.length) {
        options.data = { id: "x_Ketidaksesuaiaan", form: "fFormInputProofOfVisitedit" };
    } else {
        options.ajax = { id: "x_Ketidaksesuaiaan", form: "fFormInputProofOfVisitedit", limit: ew.LOOKUP_PAGE_SIZE };
    }
    options.minimumResultsForSearch = Infinity;
    options = Object.assign({}, ew.selectOptions, options, ew.vars.tables.FormInputProofOfVisit.fields.Ketidaksesuaiaan.selectOptions);
    ew.createSelect(options);
});
</script>
</span></template>
</div></div>
    </div>
} @* formInputProofOfVisitEdit.Ketidaksesuaiaan.Visible (end) *@
@if (formInputProofOfVisitEdit.Keterangan.Visible) { // formInputProofOfVisitEdit.Keterangan.Visible (begin)
    <div id="r_Keterangan" @Html.Raw(formInputProofOfVisitEdit.Keterangan.RowAttributes)>
        <label id="elh_FormInputProofOfVisit_Keterangan" for="x_Keterangan" class="@(formInputProofOfVisitEdit.LeftColumnClass)"><template id="tpc_FormInputProofOfVisit_Keterangan">@Html.Raw(formInputProofOfVisitEdit.Keterangan.Caption)@Html.Raw(formInputProofOfVisitEdit.Keterangan.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(formInputProofOfVisitEdit.RightColumnClass)"><div @Html.Raw(formInputProofOfVisitEdit.Keterangan.CellAttributes)>
<template id="tpx_FormInputProofOfVisit_Keterangan"><span id="el_FormInputProofOfVisit_Keterangan">
<textarea data-table="FormInputProofOfVisit" data-field="x_Keterangan" name="x_Keterangan" id="x_Keterangan" cols="35" rows="4" placeholder="@Html.Raw(HtmlEncode(formInputProofOfVisitEdit.Keterangan.PlaceHolder))"@Html.Raw(formInputProofOfVisitEdit.Keterangan.EditAttributes) aria-describedby="x_Keterangan_help">@(formInputProofOfVisitEdit.Keterangan.EditValue)</textarea>
@Html.Raw(formInputProofOfVisitEdit.Keterangan.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(formInputProofOfVisitEdit.Keterangan.GetErrorMessage())</div>
</span></template>
</div></div>
    </div>
} @* formInputProofOfVisitEdit.Keterangan.Visible (end) *@
@if (formInputProofOfVisitEdit.IdPosition.Visible) { // formInputProofOfVisitEdit.IdPosition.Visible (begin)
    <div id="r_IdPosition" @Html.Raw(formInputProofOfVisitEdit.IdPosition.RowAttributes)>
        <label id="elh_FormInputProofOfVisit_IdPosition" for="x_IdPosition" class="@(formInputProofOfVisitEdit.LeftColumnClass)"><template id="tpc_FormInputProofOfVisit_IdPosition">@Html.Raw(formInputProofOfVisitEdit.IdPosition.Caption)@Html.Raw(formInputProofOfVisitEdit.IdPosition.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(formInputProofOfVisitEdit.RightColumnClass)"><div @Html.Raw(formInputProofOfVisitEdit.IdPosition.CellAttributes)>
<template id="tpx_FormInputProofOfVisit_IdPosition"><span id="el_FormInputProofOfVisit_IdPosition">
<input type="@(formInputProofOfVisitEdit.IdPosition.InputTextType)" name="x_IdPosition" id="x_IdPosition" data-table="FormInputProofOfVisit" data-field="x_IdPosition" value="@Html.Raw(formInputProofOfVisitEdit.IdPosition.EditValue)" size="30" placeholder="@Html.Raw(HtmlEncode(formInputProofOfVisitEdit.IdPosition.PlaceHolder))" data-format-pattern="@(formInputProofOfVisitEdit.IdPosition.FormatPattern)"@Html.Raw(formInputProofOfVisitEdit.IdPosition.EditAttributes) aria-describedby="x_IdPosition_help">
@Html.Raw(formInputProofOfVisitEdit.IdPosition.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(formInputProofOfVisitEdit.IdPosition.GetErrorMessage())</div>
</span></template>
</div></div>
    </div>
} @* formInputProofOfVisitEdit.IdPosition.Visible (end) *@
</div><!-- /page* -->
<div id="tpd_FormInputProofOfVisitedit" class="ew-custom-template"></div>
<template id="tpm_FormInputProofOfVisitedit">
<div id="ct_formInputProofOfVisitEdit">@{
    string NoReferensi = "";
    List<Dictionary<string, object>> dokumenList = new List<Dictionary<string, object>>();
    int idx = 0;
    var path = HttpContext.Request.Path.Value;
    var segments = path.Split('/', StringSplitOptions.RemoveEmptyEntries);
    string id = segments.Length > 1 ? segments[1] : null;
    string sqlData = "SELECT id, FileName FROM DetailUploadPOV WHERE idPOV = @idPOV;";
    using (SqlConnection sqlConnection = GetConnection("Db").OpenConnection()) {
        using (SqlCommand sqlCommand = new SqlCommand(sqlData, sqlConnection))
        {
            sqlCommand.Parameters.AddWithValue("@idPOV", id ?? "");
            using (SqlDataReader rs = sqlCommand.ExecuteReader()) {
                while (rs.Read()) {
                    Dictionary<string, object> row = new Dictionary<string, object>();
                    for (int i = 0; i < rs.FieldCount; i++) {
                        row[rs.GetName(i)] = rs[i] != null? rs[i] : "";
                    }
                    dokumenList.Add(row);
                }
            }
        }
        sqlData = "SELECT TOP 1 NoReferensi FROM FormInputProofOfVisit WHERE id = @id";
        using (SqlCommand sqlCommand = new SqlCommand(sqlData, sqlConnection))
        {
            sqlCommand.Parameters.AddWithValue("@id", id ?? "");
            NoReferensi = sqlCommand.ExecuteScalar().ToString();
        }
    }
    var jsonDokumen = JsonConvert.SerializeObject(dokumenList);
}
<style>
    .ew-buttons {
        display: none;
    }
    .view-page-table td {
        white-space: normal !important;
        word-wrap: break-word;
    }
    .view-page-table th {
        background-color: #006BB7;
    }
    .body-table {
        background-color: #fff;
    }
    .header-table {
        background-color: #006BB7 !important;
        color: #fff !important;
    }
    .view-page-table {
        table-layout: fixed;
    }
    .btn {
        white-space: nowrap;
    }
    .file-input-wrapper {
        border: 1px solid #ced4da;
        border-radius: .375rem;
        overflow: hidden;
        transition: border-color .15s, box-shadow .15s;
    }
    .file-input-wrapper:focus-within {
        border-color: #86b7fe;
        box-shadow: 0 0 0 .25rem rgba(13,110,253,.25);
    }
    .file-input-wrapper .btn {
        border-radius: 0;
    }
    .file-input-wrapper .form-control {
        border: none;
        box-shadow: none !important;
    }
</style>
<form id="CustomFormPOV" autocomplete="off">
    <div class="row mb-3">
        <div class="col-2 fw-bold d-flex align-items-center gap-2">
            @Html.Raw(formInputProofOfVisitEdit.NoReferensi.Caption)
            <span class="text-danger">*</span>
        </div>
        <div class="col-10">
            <input type="text" class="form-control-plaintext" id="NoReferensi-custom" name="NoReferensi-custom" readonly value="@(NoReferensi)">
        </div>
    </div>
    <div class="row mb-3">
        <div class="col-2 fw-bold d-flex align-items-center gap-2">
            @Html.Raw(formInputProofOfVisitEdit.Tanggal.Caption)
            <span class="text-danger">*</span>
        </div>
        <div class="col-10">
            <slot class="ew-slot" name="tpx_FormInputProofOfVisit_Tanggal"></slot>
        </div>
    </div>
    <div class="row mb-3">
        <div class="col-2 fw-bold d-flex align-items-center gap-2">
            @Html.Raw(formInputProofOfVisitEdit.Lokasi.Caption)
            <span class="text-danger">*</span>
        </div>
        <div class="col-10">
            <slot class="ew-slot" name="tpx_FormInputProofOfVisit_Lokasi"></slot>
        </div>
    </div>
    <div class="row mb-3">
        <div class="col-2 fw-bold d-flex gap-2">
            @Html.Raw(formInputProofOfVisitEdit.UploadFoto.Caption)
            <span class="text-danger">*</span>
        </div>
        <div class="col-10">
            <div class="input-group file-input-wrapper">
                <label class="btn btn-primary mb-0">
                    Add Images <input type="file" id="fileInput" hidden multiple>
                </label>
                <input type="text" class="form-control" style="cursor: pointer;" id="fileName" value="Add Images" readonly>
            </div>
            <small class="text-muted">Maximum file size: 20 MB</small>
            <div class="border rounded p-0 mt-2" id="row-table" style="display: none;">
                <table aria-hidden="true" class="table table-hover view-page-table w-100 m-0" id="fileTable">
                    <thead>
                        <tr>
                            <th width="5%" class="fw-bold header-table">No</th>
                            <th class="fw-bold header-table">Nama File</th>
                            <th width="10%" class="fw-bold header-table"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- js render -->
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <div class="row mb-3">
        <div class="col-2 fw-bold d-flex">
            @Html.Raw(formInputProofOfVisitEdit.Keterangan.Caption)
        </div>
        <div class="col-10">
            <slot class="ew-slot" name="tpx_FormInputProofOfVisit_Keterangan"></slot>
        </div>
    </div>
    <div class="row mb-3">
        <div class="col-2 fw-bold d-flex align-items-center">
        </div>
        <div class="col-10">
            <button id="btn-submit-custom" class="btn btn-primary ew-btn" type="button">
                <i class="fas fa-save me-2"></i>Save
            </button>
            <button id="btn-cancel-custom" class="btn btn-default ew-btn" type="button">
                <i class="fas fa-ban me-2"></i>cancel
            </button>
        </div>
    </div>
</form>
<script>
    $(document).ready(function() {
        const parent_url = ew.PATH_BASE;
        const NoReferensi = "@NoReferensi";
        const id = "@id";
        const dataDokumen = @Html.Raw(jsonDokumen);
        let filesArr = [];
        let deleteIds = [];
        if (!NoReferensi || !id) {
            ew.alert('URL Tidak Valid');
            setTimeout(function() {
                history.back();
            }, 1000);
        }
        dataDokumen.forEach(item => {
            if (item.FileName) {
                filesArr.push({
                    id: item.id,
                    FileName: item.FileName
                });
            }
        });
        renderTable();
        $("#fileName").on("click", function () {
            $("#fileInput").trigger("click");
        });
        $("#fileInput").on("change", function (e) {
            let newFiles = Array.from(e.target.files);
            let mapped = [];
            for (let f of newFiles) {
                if (f.size > 20 * 1024 * 1024) {
                    ew.alert(`File "${f.name}" melebihi 20 MB dan tidak dapat diupload.`);
                    continue;
                }
                mapped.push({
                    file: f,
                    FileName: f.name
                });
            }
            if (mapped.length > 0) {
                filesArr = filesArr.concat(mapped);
                renderTable();
            }
            $(this).val('');
        });
        function renderTable() {
            let $table = $("#fileTable");
            let $tbody = $table.find("tbody");
            $tbody.empty();
            if (filesArr.length === 0) {
                $('#row-table').hide();
                return;
            }
            $('#row-table').show();
            filesArr.forEach((item, index) => {
                let FileEl = item.id ? `<button class="btn text-primary btn-download p-0" type="button" data-id="${item.id}">${item.FileName}</button>` : item.FileName;
                let row = `
                    <tr>
                        <td>${index + 1}</td>
                        <td>${FileEl}</td>
                        <td>
                            <button class="btn text-danger btn-delete" 
                                    type="button" 
                                    data-index="${index}" 
                                    data-id="${item.id || ''}">
                                <i class="fa-solid fa-times"></i>
                            </button>
                        </td>
                    </tr>
                `;
                $tbody.append(row);
            });
        }
        $(document).on("click", ".btn-delete", function () {
            let index = $(this).data("index");
            let id = $(this).data("id");
            if (id) {
                deleteIds.push(id);
            }
            filesArr.splice(index, 1);
            renderTable();
        });
        $(document).on("click", "#btn-cancel-custom", function () {
            window.location.href = `${parent_url}FormInputProofOfVisitList?cmd=search&x_NoReferensi%5B%5D=${NoReferensi}&search=&searchtype=`;
        });
        $("#btn-submit-custom").on("click", function () {
            if (filesArr.length === 0 && deleteIds.length === 0) {
                ew.alert("Tidak ada perubahan file!");
                return;
            }
            if($('#NoReferensi-custom').val() === ''){
                ew.alert("Nomor Proof of Visit tidak boleh kosong!");
                return;
            }
            if($('#x_Tanggal').val() === ''){
                ew.alert("Tanggal tidak boleh kosong!");
                return;
            }
            if($('#x_Lokasi').val() === ''){
                ew.alert("Lokasi tidak boleh kosong!");
                return;
            }
            showLoading();
            let formData = new FormData();
            formData.append("idRef", id);
            formData.append("NoReferensi", $('#NoReferensi-custom').val());
            formData.append("Tanggal", $('#x_Tanggal').val());
            formData.append("Lokasi", $('#x_Lokasi').val());
            formData.append("Keterangan", $('#x_Keterangan').val());
            filesArr.forEach(item => {
                if (item.file) {
                    formData.append("files", item.file);
                }
            });
            formData.append("Menu", "POV");
            deleteIds.forEach(id => {
                formData.append("DeletedIds", id);
            });
            $.ajax({
                url: parent_url + "api/LookUpList/UpdateDocumentGHKandPOV",
                type: "POST",
                data: formData,
                processData: false,
                contentType: false,
                success: function (rs) {
                    hideLoading();
                    if (rs.success) {
                        ew.alert(rs.message);
                        setTimeout(function () {
                            window.location.href = `${parent_url}FormInputProofOfVisitList?cmd=search&x_NoReferensi%5B%5D=${NoReferensi}&search=&searchtype=`;
                        }, 2000);
                    } else {
                        ew.alert(rs.message);
                    }
                },
                error: function (xhr) {
                    hideLoading();
                    ew.alert("Upload gagal: " + xhr.responseText);
                }
            });
        });
        $(document).on('click', '.btn-download', function (e) {
            e.preventDefault();
            const id = $(this).data('id');
            if (!id) {
                ew.alert("ID dokumen tidak ditemukan.");
                return;
            }
            $.ajax({
                url: parent_url + 'api/LookUpList/DownloadSingleDocument',
                type: 'GET',
                data: { 
                    id: id,
                    menu: 'POV'
                },
                xhrFields: {
                    responseType: 'blob'
                },
                beforeSend: function () {
                    showLoading();
                },
                success: function (response, status, xhr) {
                    const disposition = xhr.getResponseHeader('Content-Disposition');
                    let fileName = "download_file";
                    if (disposition) {
                        const fileNameMatch = disposition.match(/filename="?([^";]+)"?/);
                        if (fileNameMatch && fileNameMatch[1]) {
                            fileName = decodeURIComponent(fileNameMatch[1]);
                        }
                        const utf8FileNameMatch = disposition.match(/filename\*\s*=\s*UTF-8''([^;]+)/);
                        if (utf8FileNameMatch && utf8FileNameMatch[1]) {
                            fileName = decodeURIComponent(utf8FileNameMatch[1]);
                        }
                    }
                    const blobUrl = window.URL.createObjectURL(response);
                    const a = document.createElement('a');
                    a.href = blobUrl;
                    a.download = fileName;
                    document.body.appendChild(a);
                    a.click();
                    document.body.removeChild(a);
                    window.URL.revokeObjectURL(blobUrl);
                    ew.alert("Download berhasil!");
                },
                error: function (xhr, status, error) {
                    if (xhr.status === 404) {
                        ew.alert("File tidak ditemukan di server.");
                    } else if (xhr.status === 500) {
                        ew.alert("Terjadi kesalahan server. Coba lagi nanti.");
                    } else {
                        ew.alert("Gagal mendownload file. Error: " + xhr.responseText);
                    }
                },
                complete: function () {
                    hideLoading();
                }
            });
        });
    });
</script></div>
</template>
@{ formInputProofOfVisitEdit.CloseRecordset(); }
@Html.Raw(formInputProofOfVisitEdit.IsModal ? "<template class=\"ew-modal-buttons\">" : "<div class=\"row ew-buttons\">")<!-- buttons .row -->
    <div class="@(formInputProofOfVisitEdit.OffsetColumnClass)"><!-- buttons offset -->
    <button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit" form="fFormInputProofOfVisitedit">@Html.Raw(Language.Phrase("SaveBtn"))</button>
@if (IsJsonResponse()) {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-bs-dismiss="modal">@Html.Raw(Language.Phrase("CancelBtn"))</button>
} else {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" form="fFormInputProofOfVisitedit" data-href="@AppPath(formInputProofOfVisitEdit.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
}
    </div><!-- /buttons offset -->
@Html.Raw(formInputProofOfVisitEdit.IsModal ? "</template>" : "</div>")<!-- /buttons .row -->
</form>
<script class="ew-apply-template">
loadjs.ready(ew.applyTemplateId, function() {
    var $ = jQuery;
    ew.templateData = { rows: @Html.Raw(ConvertToJson(formInputProofOfVisitEdit.Rows)) };
    ew.applyTemplate("tpd_FormInputProofOfVisitedit", "tpm_FormInputProofOfVisitedit", "FormInputProofOfVisitedit", "@(formInputProofOfVisitEdit.Export)", "FormInputProofOfVisit", ew.templateData.rows[0], @(formInputProofOfVisitEdit.IsModal ? "true" : "false"));
    loadjs.done("customtemplate");
});
</script>
</main>
@formInputProofOfVisitEdit.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("FormInputProofOfVisit");
});
</script>
<script>
loadjs.ready("load", function() {
    // Startup script
    // Write your table-specific startup script here
    // document.write("page loaded");
    const parent_url = ew.PATH_BASE;

    /* --- Ambil NoReferensi (prioritas dari input readonly) --- */
    function resolveNoRefEdit() {
        // 1) Dari input readonly yang kamu tampilkan di Edit
        let v = ($("#NoReferensi-custom").val() || "").trim();
        if (v) return v;

        // 2) Fallback dari URL / form jika suatu saat berubah
        const qs = new URLSearchParams(location.search);
        v = (qs.getAll("NoReferensi")[0] || qs.getAll("x_NoReferensi[]")[0] || "").trim();
        if (!v) v = (qs.get("x_NoReferensi") || "").trim();
        if (!v) v = ($('input[name="x_NoReferensi[]"]').val() || $('input[name="x_NoReferensi"]').val() || "").trim();
        return v;
    }
    const NoReferensi = resolveNoRefEdit();

    /* --- Guard akses: OH (Rule 10) bebas; lainnya harus MOD (API GetMODForPOV) --- */
    (function () {
        const role = parseInt('@CurrentUserInfo("Rule")') || 0; // hanya OH yang auto-lolos
        if (role == 0 || role === 10) return;
        const noRef = NoReferensi;
        if (!noRef) return; // jika NoRef belum ada (edge), biarkan default

        // id form edit (sesuaikan jika generator memberi id berbeda)
        const $form = $("#fFormInputProofOfVisitedit").length ?
            $("#fFormInputProofOfVisitedit") :
            $("form.ew-form").first();
        if (!$form.length) return;
        const backUrl = `${parent_url}FormInputProofOfVisitList?cmd=search&x_NoReferensi%5B%5D=${encodeURIComponent(noRef)}`;
        const denyAccess = (msg) => {
            const $cancelCandidates = $(
                '#btn-cancel-custom, a[data-ew-action="cancel"], button[name="btn-cancel"], a.btn-secondary, a.btn-default'
            );

            // 1) Disable semua kontrol KECUALI Cancel
            $form.find("input,select,textarea,button")
                .not($cancelCandidates)
                .prop("disabled", true);

            // 2) Sembunyikan tombol aksi utama (Save/Submit)
            $form.find('button[name="btn-action"], button[name="btn-submit"], .ew-btn[data-ew-action]')
                .not($cancelCandidates)
                .hide();

            // 3) Aktifkan & arahkan Cancel → backUrl
            const enableCancel = ($el) => {
                if (!$el || !$el.length) return;
                if ($el.is("a")) {
                    $el.attr("href", backUrl);
                } else {
                    $el.off("click").on("click", function (e) {
                        e.preventDefault();
                        window.location.href = backUrl;
                    });
                }
                $el.prop("disabled", false).removeAttr("disabled").removeClass("disabled").css("pointer-events", "auto").show();
            };
            $cancelCandidates.each(function () {
                enableCancel($(this));
            });

            // Fallback Cancel jika tidak ada sama sekali
            if (!$cancelCandidates.length && !$("#btn-cancel-fallback").length) {
                const $fallback = $(
                    `<a class="btn btn-secondary ms-2" id="btn-cancel-fallback" href="${backUrl}">
                    <i class="fa-solid fa-xmark"></i> Cancel
                    </a>`
                );
                const $btnWrap = $form.find('button[name="btn-submit"]').first().parent();
                ($btnWrap.length ? $btnWrap : $(".ew-toolbar").first()).append($fallback);
            }

            // 4) Alert peringatan
            const $container = $(".content .container-fluid").first();
            const $alert = $(`
            <div class="alert alert-warning d-flex align-items-start mt-3" role="alert" id="no-access-alert">
                <i class="fa-solid fa-triangle-exclamation me-2 mt-1"></i>
                <div>
                <div class="fw-bold">Tidak ada akses untuk mengedit Form Input Proof of Visit.</div>
                <div class="small">
                    ${msg || "Akses dibatasi: hanya User MOD / OH / Admin yang dapat mengedit data."}
                    <br>Solusi: <em>Ganti Akun</em> atau <em>minta diberikan akses oleh OH</em>.
                </div>
                <div class="mt-2">
                    <a href="${parent_url}logout" class="btn btn-outline-secondary btn-sm me-2">
                    <i class="fa-solid fa-right-from-bracket"></i> Ganti Akun
                    </a>
                    <a href="${backUrl}" class="btn btn-primary btn-sm">
                    <i class="fa-solid fa-list"></i> Kembali ke List
                    </a>
                </div>
                </div>
            </div>
            `);
            if ($container.length) $container.prepend($alert);
            else $("body").prepend($alert);

            // Pastikan cancel custom (jika ada) benar-benar aktif
            setTimeout(() => {
                $("#btn-cancel-custom").prop("disabled", false).removeAttr("disabled").removeClass("disabled");
            }, 0);
            window.scrollTo({
                top: 0,
                behavior: "smooth"
            });
        };

        // Cek MOD via API
        $.get(ew.PATH_BASE + "api/LookUpList/GetMODForPOV", {
                noRef
            })
            .done(function (res) {
                if ((!res || res.success !== true || res.isCurrentUserMOD !== true) && parseInt('@CurrentUserInfo("UserLevel")') != 4) {
                    denyAccess("Anda bukan User MOD untuk plant & tanggal ini. Minta akses ke OH atau gunakan akun yang tepat.");
                }
            })
            .fail(function () {
                denyAccess("Gagal memverifikasi MOD. Hubungi Administrator / OH.");
            });
    })();
});
</script>
