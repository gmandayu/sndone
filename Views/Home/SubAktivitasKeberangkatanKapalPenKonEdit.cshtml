@{
    // View
    View = this;

    // Check action result
    if (subAktivitasKeberangkatanKapalPenKonEdit.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = subAktivitasKeberangkatanKapalPenKonEdit.UseLayout ? "_Layout" : "";
}
@subAktivitasKeberangkatanKapalPenKonEdit.ShowPageHeader()
@subAktivitasKeberangkatanKapalPenKonEdit.ShowMessages()
<main class="edit">
<form name="fSubAktivitasKeberangkatanKapalPenKonedit" id="fSubAktivitasKeberangkatanKapalPenKonedit" class="@(subAktivitasKeberangkatanKapalPenKonEdit.FormClassName)" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
<script>
var currentTable = @Html.Raw(ConvertToJson(subAktivitasKeberangkatanKapalPenKonEdit.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { SubAktivitasKeberangkatanKapalPenKon: currentTable } });
var currentPageID = ew.PAGE_ID = "edit";
var currentForm;
var fSubAktivitasKeberangkatanKapalPenKonedit;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fSubAktivitasKeberangkatanKapalPenKonedit")
        .setPageId("edit")

        // Add fields
        .setFields([

            ["idProses", [fields.idProses.visible && fields.idProses.required ? ew.Validators.required(fields.idProses.caption) : null], fields.idProses.isInvalid],
            ["idAktifitas", [fields.idAktifitas.visible && fields.idAktifitas.required ? ew.Validators.required(fields.idAktifitas.caption) : null], fields.idAktifitas.isInvalid],
            ["LastUpdatedBy", [fields.LastUpdatedBy.visible && fields.LastUpdatedBy.required ? ew.Validators.required(fields.LastUpdatedBy.caption) : null], fields.LastUpdatedBy.isInvalid],
            ["lastUpdatedDate", [fields.lastUpdatedDate.visible && fields.lastUpdatedDate.required ? ew.Validators.required(fields.lastUpdatedDate.caption) : null], fields.lastUpdatedDate.isInvalid],
            ["StatusAktivitas", [fields.StatusAktivitas.visible && fields.StatusAktivitas.required ? ew.Validators.required(fields.StatusAktivitas.caption) : null], fields.StatusAktivitas.isInvalid],
            ["NoReferensi", [fields.NoReferensi.visible && fields.NoReferensi.required ? ew.Validators.required(fields.NoReferensi.caption) : null], fields.NoReferensi.isInvalid],
            ["Tanggal", [fields.Tanggal.visible && fields.Tanggal.required ? ew.Validators.required(fields.Tanggal.caption) : null, ew.Validators.datetime(fields.Tanggal.clientFormatPattern)], fields.Tanggal.isInvalid]
        ])

        // Form_CustomValidate
        .setCustomValidate(
            function(fobj) { // DO NOT CHANGE THIS LINE!
                    // Your custom validation code here, return false if invalid.
                    return true;
                }
        )

        // Use JavaScript validation or not
        .setValidateRequired(ew.CLIENT_VALIDATE)

        // Dynamic selection lists
        .setLists({
        })
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Client script
    // Write your client script here, no need to add script tags.
    var parent_url = ew.PATH_BASE;
    var IdProses = "@subAktivitasKeberangkatanKapalPenKonEdit.idProses.CurrentValue";
    function handleCustomSubmit(status) {
        let idAktivitas = $("#x_idAktifitas").val();
        let originalStatus = $("#el_SubAktivitasKeberangkatanKapalPenKon_StatusAktivitas .form-control-plaintext").text().trim();
        $.ajax({
            url: parent_url + `api/LookUpList/check_valid_status_aktivitas?idAktivitas=${idAktivitas}&originalStatus=${originalStatus}`,
            type: 'GET',
            beforeSend: function () {
                showLoading();
            },
            success: function (response) {
                if (!response.IsValid){
                    hideLoading();
                    ew.alert(response.Message ?? "Status aktivitas telah diubah oleh pengguna lain");
                    return;
                } else {
                    let form = $("#fSubAktivitasKeberangkatanKapalPenKonedit");
                    $.ajax({
                        url: form.attr("action"),
                        type: form.attr("method"),
                        data: form.serialize(),
                        success: function (response) {
                            updateStatus(idAktivitas, status);
                        },
                        error: function (xhr, status, error) {
                            hideLoading();
                            ew.alert("Gagal menyimpan data.");
                            console.error("Form submit gagal:", error);
                        }
                    });  
                }
            },
            error: function (xhr, status, error) {
                hideLoading();
                ew.alert("Gagal menyimpan data.");
                console.error("Form submit gagal:", error);
            }
        }); 
    }
    function updateStatus(idAktivitas, status) {
        $.ajax({
            url: parent_url + `api/LookUpList/update_status_aktivitas?idAktivitas=${idAktivitas}&statusAktivitas=${status}`,
            type: 'POST',
            success: function () {
                console.log("Status berhasil diperbarui.");
                if (status == "Completed" || status == "Completed Edit") {
                    window.location.href = `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`;
                } else {
                    location.reload();
                }
            },
            error: function (xhr, status, error) {
                ew.alert("Status gagal diperbarui.");
                console.error("Update status gagal:", error);
            },
            complete: function () {
                hideLoading();
            }
        });
    }
    $(document).on("click", "#btn-draft", function () {
        handleCustomSubmit("Draft");
    });
    $(document).on("click", "#btn-completed", function () {
        handleCustomSubmit("Completed");
    });
    $(document).on("click", "#btn-editing", function () {
        handleCustomSubmit("Editing");
    });
    $(document).on("click", "#btn-completedEdit", function () {
        handleCustomSubmit("Completed Edit");
    });
});
</script>
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="update">
<input type="hidden" name="modal" value="@ConvertToInt(subAktivitasKeberangkatanKapalPenKonEdit.IsModal)">
@if (IsJsonResponse()) {
<input type="hidden" name="json" value="1">
}
<input type="hidden" name="@(subAktivitasKeberangkatanKapalPenKonEdit.OldKeyName)" value="@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.OldKey)">
<div class="ew-edit-div"><!-- page* -->
@if (subAktivitasKeberangkatanKapalPenKonEdit.idProses.Visible) { // subAktivitasKeberangkatanKapalPenKonEdit.idProses.Visible (begin)
    <div id="r_idProses" @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idProses.RowAttributes)>
        <label id="elh_SubAktivitasKeberangkatanKapalPenKon_idProses" class="@(subAktivitasKeberangkatanKapalPenKonEdit.LeftColumnClass)">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idProses.Caption)@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idProses.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.RightColumnClass)"><div @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idProses.CellAttributes)>
<span id="el_SubAktivitasKeberangkatanKapalPenKon_idProses">
<span @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idProses.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idProses.DisplayValue(subAktivitasKeberangkatanKapalPenKonEdit.idProses.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_idProses" data-hidden="1" name="x_idProses" id="x_idProses" value="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.idProses.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasKeberangkatanKapalPenKonEdit.idProses.Visible (end) *@
@if (subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.Visible) { // subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.Visible (begin)
    <div id="r_idAktifitas" @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.RowAttributes)>
        <label id="elh_SubAktivitasKeberangkatanKapalPenKon_idAktifitas" class="@(subAktivitasKeberangkatanKapalPenKonEdit.LeftColumnClass)">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.Caption)@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.RightColumnClass)"><div @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.CellAttributes)>
<span id="el_SubAktivitasKeberangkatanKapalPenKon_idAktifitas">
<span @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.DisplayValue(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_idAktifitas" data-hidden="1" name="x_idAktifitas" id="x_idAktifitas" value="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.Visible (end) *@
@if (subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.Visible) { // subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.Visible (begin)
    <div id="r_LastUpdatedBy" @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.RowAttributes)>
        <label id="elh_SubAktivitasKeberangkatanKapalPenKon_LastUpdatedBy" for="x_LastUpdatedBy" class="@(subAktivitasKeberangkatanKapalPenKonEdit.LeftColumnClass)">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.Caption)@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.RightColumnClass)"><div @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.CellAttributes)>
<span id="el_SubAktivitasKeberangkatanKapalPenKon_LastUpdatedBy">
<span @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_LastUpdatedBy" data-hidden="1" name="x_LastUpdatedBy" id="x_LastUpdatedBy" value="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasKeberangkatanKapalPenKonEdit.LastUpdatedBy.Visible (end) *@
@if (subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.Visible) { // subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.Visible (begin)
    <div id="r_lastUpdatedDate" @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.RowAttributes)>
        <label id="elh_SubAktivitasKeberangkatanKapalPenKon_lastUpdatedDate" for="x_lastUpdatedDate" class="@(subAktivitasKeberangkatanKapalPenKonEdit.LeftColumnClass)">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.Caption)@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.RightColumnClass)"><div @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.CellAttributes)>
<span id="el_SubAktivitasKeberangkatanKapalPenKon_lastUpdatedDate">
<span @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_lastUpdatedDate" data-hidden="1" name="x_lastUpdatedDate" id="x_lastUpdatedDate" value="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasKeberangkatanKapalPenKonEdit.lastUpdatedDate.Visible (end) *@
@if (subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.Visible) { // subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.Visible (begin)
    <div id="r_StatusAktivitas" @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.RowAttributes)>
        <label id="elh_SubAktivitasKeberangkatanKapalPenKon_StatusAktivitas" class="@(subAktivitasKeberangkatanKapalPenKonEdit.LeftColumnClass)">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.Caption)@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.RightColumnClass)"><div @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.CellAttributes)>
<span id="el_SubAktivitasKeberangkatanKapalPenKon_StatusAktivitas">
<span @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.DisplayValue(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_StatusAktivitas" data-hidden="1" name="x_StatusAktivitas" id="x_StatusAktivitas" value="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasKeberangkatanKapalPenKonEdit.StatusAktivitas.Visible (end) *@
@if (subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.Visible) { // subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.Visible (begin)
    <div id="r_NoReferensi" @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.RowAttributes)>
        <label id="elh_SubAktivitasKeberangkatanKapalPenKon_NoReferensi" for="x_NoReferensi" class="@(subAktivitasKeberangkatanKapalPenKonEdit.LeftColumnClass)">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.Caption)@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.RightColumnClass)"><div @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.CellAttributes)>
<span id="el_SubAktivitasKeberangkatanKapalPenKon_NoReferensi">
<span @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_NoReferensi" data-hidden="1" name="x_NoReferensi" id="x_NoReferensi" value="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.Visible (end) *@
@if (subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.Visible) { // subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.Visible (begin)
    <div id="r_Tanggal" @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.RowAttributes)>
        <label id="elh_SubAktivitasKeberangkatanKapalPenKon_Tanggal" for="x_Tanggal" class="@(subAktivitasKeberangkatanKapalPenKonEdit.LeftColumnClass)">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.Caption)@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.RightColumnClass)"><div @Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.CellAttributes)>
<span id="el_SubAktivitasKeberangkatanKapalPenKon_Tanggal">
<input type="@(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.InputTextType)" name="x_Tanggal" id="x_Tanggal" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_Tanggal" value="@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.EditValue)" placeholder="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.PlaceHolder))" data-format-pattern="@(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.FormatPattern)"@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.EditAttributes) aria-describedby="x_Tanggal_help">
@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.GetErrorMessage())</div>
@if (!subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.ReadOnly && !subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.Disabled && !subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.EditAttrs.Is("readonly") && !subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.EditAttrs.Is("disabled")) {
<script>
loadjs.ready(["fSubAktivitasKeberangkatanKapalPenKonedit", "datetimepicker"], function () {
    let format = "@Html.Raw(DateFormat(9))", // DN
        options = {
                localization: {
                    locale: ew.LANGUAGE_ID + "-u-nu-" + ew.getNumberingSystem(),
                    hourCycle: format.match(/H/) ? "h24" : "h12",
                    format,
                    ...ew.language.phrase("datetimepicker")
                },
                display: {
                    icons: {
                        previous: ew.IS_RTL ? "fa-solid fa-chevron-right" : "fa-solid fa-chevron-left",
                        next: ew.IS_RTL ? "fa-solid fa-chevron-left" : "fa-solid fa-chevron-right"
                    },
                    components: {
                        clock: !!format.match(/h/i) || !!format.match(/m/) || !!format.match(/s/i),
                        hours: !!format.match(/h/i),
                        minutes: !!format.match(/m/),
                        seconds: !!format.match(/s/i)
                    },
                theme: ew.getPreferredTheme()
                }
            };
    ew.createDateTimePicker("fSubAktivitasKeberangkatanKapalPenKonedit", "x_Tanggal", ew.deepAssign({"useCurrent":false,"display":{"sideBySide":false}}, options));
});
</script>
}
</span>
</div></div>
    </div>
} @* subAktivitasKeberangkatanKapalPenKonEdit.Tanggal.Visible (end) *@
</div><!-- /page* -->
    <input type="hidden" data-table="SubAktivitasKeberangkatanKapalPenKon" data-field="x_id" data-hidden="1" name="x_id" id="x_id" value="@Html.Raw(HtmlEncode(subAktivitasKeberangkatanKapalPenKonEdit.id.CurrentValue))">
@{ subAktivitasKeberangkatanKapalPenKonEdit.CloseRecordset(); }
@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.IsModal ? "<template class=\"ew-modal-buttons\">" : "<div class=\"row ew-buttons\">")<!-- buttons .row -->
    <div class="@(subAktivitasKeberangkatanKapalPenKonEdit.OffsetColumnClass)"><!-- buttons offset -->
    <button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit" form="fSubAktivitasKeberangkatanKapalPenKonedit">@Html.Raw(Language.Phrase("SaveBtn"))</button>
@if (IsJsonResponse()) {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-bs-dismiss="modal">@Html.Raw(Language.Phrase("CancelBtn"))</button>
} else {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" form="fSubAktivitasKeberangkatanKapalPenKonedit" data-href="@AppPath(subAktivitasKeberangkatanKapalPenKonEdit.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
}
    </div><!-- /buttons offset -->
@Html.Raw(subAktivitasKeberangkatanKapalPenKonEdit.IsModal ? "</template>" : "</div>")<!-- /buttons .row -->
</form>
</main>
@subAktivitasKeberangkatanKapalPenKonEdit.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("SubAktivitasKeberangkatanKapalPenKon");
});
</script>
<script>
loadjs.ready("load", function() {
    // Startup script
    // Write your table-specific startup script here
    // document.write("page loaded");
    var IdReferensi = "@subAktivitasKeberangkatanKapalPenKonEdit.NoReferensi.CurrentValue";
    var IdProses = "@subAktivitasKeberangkatanKapalPenKonEdit.idProses.CurrentValue";
    var idAktivitas = "@subAktivitasKeberangkatanKapalPenKonEdit.idAktifitas.CurrentValue";
    var NamaAktivitas = $('#el_SubAktivitasPemberangkatanKapalPenKon_idAktifitas').text();
    var parent_url = ew.PATH_BASE;
    var steps = [
        { number: 1, label: "Proses Penyaluran", url: `${parent_url}PenyaluranList`, icon: "fa-truck-fast" },
        { number: 2, label: "Proses", url: `${parent_url}ProsesList?cmd=search&x_IdReferensi%5B%5D=${IdReferensi}&search=&searchtype=`, icon: "fa-gears" },
        { number: 3, label: "Aktivitas", url: `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`, icon: "fa-list-check" },
        { number: 4, label: NamaAktivitas, url: '', icon: "fa-file-circle-check" }
    ];
    var currentStep = 4;
    $("<style>")
        .prop("type", "text/css")
        .html(`
            .stepper {
                display: flex;
                counter-reset: step;
                flex-wrap: nowrap;        /* jangan pecah ke bawah */
                overflow-x: auto;         /* scroll horizontal */
                -webkit-overflow-scrolling: touch; /* smooth scroll di mobile */
                padding-bottom: 8px;      /* space biar scroll bar gak nempel */
            }
            .stepper::-webkit-scrollbar {
            height: 6px; /* tipis */
            }
            .stepper::-webkit-scrollbar-thumb {
                background-color: #ccc;
                border-radius: 3px;
            }
            .step {
                width: 150px;
                text-align: center;
                position: relative;
            }
            .step:not(:last-child)::after {
                content: '';
                position: absolute;
                top: 20px;
                right: -50%;
                width: 100%;
                height: 4px;
                background-color: #ccc;
                z-index: 0;
            }
            .step.completed:not(:last-child)::after {
                background-color: #3c6dbd;
            }
            .step-icon {
                width: 36px;
                height: 36px;
                line-height: 36px;
                border-radius: 50%;
                background: #ccc;
                display: inline-block;
                z-index: 1;
                position: relative;
                color: white;
                font-weight: bold;
            }
            .step.active .step-icon {
                background: #3c6dbd;
            }
            .step-label {
                margin-top: 8px;
                font-size: 14px;
                display: flex;
                justify-content: center;
                align-items: center;
                gap: 6px;
                color: #333;
            }
            .step-label {
                color: #3c6dbd !important;
                text-decoration: none;
                margin: 0;
                padding: 0;
            }
            .step-label-not-active {
                color: #CCCCCC !important;
            }
            .ag-theme-quartz .ag-header {
                background-color: #3c6dbd !important;
            }
            .ag-theme-quartz .ag-header-cell-label {
                color: #ffffff !important;
            }
            .ag-theme-quartz .ag-icon-filter {
                color: #ffffff !important;
            }
            .rotate-down {
                transform: rotate(90deg);
                transition: transform 0.3s ease;
            }
            .rotate-right {
                transform: rotate(0deg);
                transition: transform 0.3s ease;
            }
        `)
        .appendTo("head");
    var $stepperWrapper = $('<div class="d-flex border-bottom border-2 pb-2"><div class="stepper"></div></div>');
    var $stepper = $stepperWrapper.find(".stepper");
    steps.forEach(function (step, i) {
        var isActive = step.number === currentStep;
        var isCompleted = step.number < currentStep;
        var stepClass = isActive ? "active" : isCompleted ? "completed" : "";
        var icon, labelContent, html;
        if (isActive) {
            icon = `<div class="step-icon bg-success">${step.number}</div>`;
            labelContent = `<div class="step-label"><p class="text-success mb-1"><i class="fa-solid ${step.icon} me-1 text-success"></i> ${step.label}</p></div>`;
            html = `<div class="step ${stepClass} text-success">${icon}${labelContent}</div>`;
        } else if (isCompleted) {
            icon = `<div class="step-icon" style="background: #3c6dbd;">${step.number}</div>`;
            labelContent = `<div class="step-label"><a href="${step.url}" class="text-primary"><i class="fa-solid ${step.icon} me-1"></i>${step.label}</a></div>`;
            html = `<div class="step ${stepClass}">${icon}${labelContent}</div>`;
        } else {
            icon = `<div class="step-icon">${step.number}</div>`;
            labelContent = `<div class="step-label-not-active"><p style="margin: 0;"><i class="fa-solid ${step.icon} me-1"></i>${step.label}</p></div>`;
            html = `<div class="step ${stepClass} text-secondary">${icon}${labelContent}</div>`;
        }
        $stepper.append(html);
    });
    $(".content-header .container-fluid .row").append($stepperWrapper);
    $("#btn-action").hide();
    $("#btn-cancel").html('<i class="fa-solid fa-ban"></i></i> Cancel');
    const $cancelButton = $("#btn-cancel");
    const idAktifitas = $("#x_idAktifitas").val();
    if ($cancelButton.length) {
        $.ajax({
            url: parent_url + `api/LookUpList/get_status_aktivitas?idAktivitas=${idAktifitas}`,
            type: 'GET',
            success: function(response) {
                let status = response.statusAktivitas;
                let $customButtons = ``;
                switch (status) {
                case "Waiting":
                    $customButtons = `
                        <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-draft">
                            <i class="fas fa-save"></i> Save
                        </button>
                    `;
                    break;
                case "Draft":
                    $customButtons = `
                        <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-draft">
                            <i class="fas fa-save"></i> Save
                        </button>
                        <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completed">
                            <i class="fas fa-paper-plane"></i> Submit
                        </button>
                    `
                    break;
                case "Completed":
                    $customButtons = `
                        <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-editing">
                            <i class="fa-solid fa-pen-to-square"></i> Edit
                        </button>
                        <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completedEdit">
                            <i class="fas fa-paper-plane"></i> Submit
                        </button>
                    `;
                    break;
                case "Editing":
                    $customButtons = `
                        <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-editing">
                            <i class="fa-solid fa-pen-to-square"></i> Edit
                        </button>
                        <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completedEdit">
                            <i class="fas fa-paper-plane"></i> Submit
                        </button>
                    `;
                    break;
                }
                    $cancelButton.before($customButtons);
                    // checkIsEditable();
            },
            error: function(xhr, status, error) {
                console.error('API call failed:', status, error);
            }
        });
    }
    let StatusAktivitas = $('#el_SubAktivitasKeberangkatanKapalPenKon_StatusAktivitas').text().trim();
    function checkIsEditable() {
        $.ajax({
            url: parent_url + `api/LookUpList/check_iseditable_aktivitas_form?idAktivitas=${idAktifitas}`,
            type: 'GET',
            success: function(response) {
                if(!response.isEditable){
                    $("#btn-draft").hide();
                    $("#btn-editing").hide();
                    $("#btn-completed").hide();
                    $("#btn-completedEdit").hide();
                    $('input, select, textarea').prop('disabled', true);
                }
            },
            error: function(xhr, status, error) {
                console.error('API call failed:', status, error);
            }
        });
    }

    // --- log aktivitas start ---
    $('#el_SubAktivitasKeberangkatanKapalPenKon_LastUpdatedBy').addClass('d-flex gap-3');
    $('#el_SubAktivitasKeberangkatanKapalPenKon_LastUpdatedBy').append(`
        <button type="button" class="btn btn-primary ew-btn ps-2" expand-log="false">
            <i class="fa-solid fa-caret-right fa-2xl me-2 caret-icon rotate-right"></i>
            Log History
            <i class="fa-solid fa-clock-rotate-left ms-1"></i> 
        </button>
    `);
    $(document).on('click', 'button[expand-log]', function () {
        const $btn = $(this);
        const isExpanded = $btn.attr('expand-log') === 'true';
        const $row = $('#log-table-row');
        const $wrapper = $('#log-table-wrapper');
        const $caret = $btn.find('.caret-icon');
        if (!isExpanded) {
            $btn.attr('expand-log', 'true');
            $row.removeClass('d-none');
            setTimeout(() => {
                $wrapper.css({
                    maxHeight: '500px'
                });
            }, 10);
            $caret.removeClass('rotate-right').addClass('rotate-down');
        } else {
            $btn.attr('expand-log', 'false');
            $wrapper.css({
                maxHeight: '0'
            });
            setTimeout(() => {
                $row.addClass('d-none');
            }, 600);
            $caret.removeClass('rotate-down').addClass('rotate-right');
        }
    });
    $.ajax({
        url: parent_url + `api/LookUpList/GetLogValueByIdAktivitas?IdAktivitas=${idAktivitas}`,
        method: 'GET',
        success: function (response) {
            const mappedData = response.result.map(item => ({
                Aktivitas: item.Aktivitas,
                Username: item.Username,
                StatusAktivitas: item.StatusAktivitas,
                Tanggal: item.Tanggal
            }));
            $.getScript('https://cdn.jsdelivr.net/npm/ag-grid-community/dist/ag-grid-community.min.js', function () {
                $('#r_LastUpdatedBy').after(`
                    <div id="log-table-row" class="d-none row m-0">
                        <div class="p-0 col-2"></div>
                        <div class="p-0 col-10">
                            <div id="log-table-wrapper" style="overflow: hidden; transition: all 0.6s ease; max-height: 0;">
                                <div id="myGrid" class="ag-theme-quartz w-100 my-3" style="height: 300px;"></div>
                            </div>
                        </div>
                    </div>
                `);
                const gridOptions = {
                    rowData: mappedData,
                    columnDefs: [
                        {
                            headerName: 'No.',
                            valueGetter: 'node.rowIndex + 1',
                            width: 80,
                            filter: false
                        },
                        {
                            headerName: 'Aktivitas',
                            field: 'Aktivitas',
                            flex: 1
                        },
                        {
                            headerName: 'Username',
                            field: 'Username',
                            flex: 1
                        },
                        {
                            headerName: 'Status Aktivitas',
                            field: 'StatusAktivitas',
                            flex: 1
                        },
                        {
                            headerName: 'Tanggal',
                            field: 'Tanggal',
                            flex: 1
                        }
                    ],
                    defaultColDef: {
                        sortable: false,
                        suppressMovable: true,
                        filter: true,
                        resizable: false
                    },
                    rowSelection: 'multiple',
                    suppressRowClickSelection: true,
                    pagination: true,
                    paginationPageSize: 10
                };
                const myGridElement = document.querySelector('#myGrid');
                agGrid.createGrid(myGridElement, gridOptions);
            });
        },
        error: function(xhr, status, error) {
            console.error('API call failed:', status, error);
        }
    });
    // --- log aktivitas end ---
});
</script>
