@{
    // View
    View = this;

    // Check action result
    if (subAktivitasPengecekanMtList.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = subAktivitasPengecekanMtList.UseLayout ? "_Layout" : "";
}
@if (!subAktivitasPengecekanMtList.IsExport()) {
<script>
var currentTable = @Html.Raw(ConvertToJson(subAktivitasPengecekanMtList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { SubAktivitasPengecekanMT: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(subAktivitasPengecekanMtList.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(subAktivitasPengecekanMtList.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(subAktivitasPengecekanMtList.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(subAktivitasPengecekanMtList.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
}
@if (!subAktivitasPengecekanMtList.IsExport()) {
<div class="btn-toolbar ew-toolbar">
@if (subAktivitasPengecekanMtList.TotalRecords > 0 && subAktivitasPengecekanMtList.ExportOptions.Visible) {
    subAktivitasPengecekanMtList.ExportOptions.Render("body");
}
@if (subAktivitasPengecekanMtList.ImportOptions.Visible) {
    subAktivitasPengecekanMtList.ImportOptions.Render("body");
}
@if (subAktivitasPengecekanMtList.SearchOptions.Visible) {
    subAktivitasPengecekanMtList.SearchOptions.Render("body");
}
@if (subAktivitasPengecekanMtList.FilterOptions.Visible) {
    subAktivitasPengecekanMtList.FilterOptions.Render("body");
}
</div>
}
@if (!subAktivitasPengecekanMtList.IsModal) {
<form name="fSubAktivitasPengecekanMTsrch" id="fSubAktivitasPengecekanMTsrch" class="ew-form ew-ext-search-form" action="@AppPath(CurrentPageName())" novalidate autocomplete="off">
<div id="fSubAktivitasPengecekanMTsrch_search_panel" class="mb-2 mb-sm-0 @(subAktivitasPengecekanMtList.SearchPanelClass)"><!-- .ew-search-panel -->
<script>
var currentTable = @Html.Raw(ConvertToJson(subAktivitasPengecekanMtList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { SubAktivitasPengecekanMT: currentTable } });
var currentForm;
var fSubAktivitasPengecekanMTsrch, currentSearchForm, currentAdvancedSearchForm;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery,
        fields = currentTable.fields;

    // Form object for search
    let form = new ew.FormBuilder()
        .setId("fSubAktivitasPengecekanMTsrch")
        .setPageId("list")
@if (subAktivitasPengecekanMtList.UseAjaxActions) {
        <text>.setSubmitWithFetch(true)</text>
}

        // Dynamic selection lists
        .setLists({
        })

        // Filters
        .setFilterList(@Html.Raw(subAktivitasPengecekanMtList.GetFilterList()))
        .build();
    window[form.id] = form;
    currentSearchForm = form;
    loadjs.done(form.id);
});
</script>
<input type="hidden" name="cmd" value="search">
@if (Security.CanSearch) {
if (!subAktivitasPengecekanMtList.IsExport() && Empty(subAktivitasPengecekanMtList.CurrentAction) && subAktivitasPengecekanMtList.HasSearchFields()) { @* Show search form (begin) *@
<div class="ew-extended-search container-fluid ps-2">
<div class="row mb-0">
    <div class="col-sm-auto px-0 pe-sm-2">
        <div class="ew-basic-search input-group">
            <input type="search" name="@(Config.TableBasicSearch)" id="@(Config.TableBasicSearch)" class="form-control ew-basic-search-keyword" value="@Html.Raw(HtmlEncode(subAktivitasPengecekanMtList.BasicSearch.SessionKeyword))" placeholder="@(HtmlEncode(Language.Phrase("Search")))" aria-label="@(HtmlEncode(Language.Phrase("Search")))">
            <input type="hidden" name="@(Config.TableBasicSearchType)" id="@(Config.TableBasicSearchType)" class="ew-basic-search-type" value="@(HtmlEncode(subAktivitasPengecekanMtList.BasicSearch.SessionType))">
            <button type="button" data-bs-toggle="dropdown" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" aria-haspopup="true" aria-expanded="false">
                <span id="searchtype">@Html.Raw(subAktivitasPengecekanMtList.BasicSearch.TypeNameShort)</span>
            </button>
            <div class="dropdown-menu dropdown-menu-end">
                <button type="button" class="dropdown-item@(subAktivitasPengecekanMtList.BasicSearch.SessionType == "" ? " active" : "")" form="fSubAktivitasPengecekanMTsrch" data-ew-action="search-type">@Html.Raw(Language.Phrase("QuickSearchAuto"))</button>
                <button type="button" class="dropdown-item@(subAktivitasPengecekanMtList.BasicSearch.SessionType == "=" ? " active" : "")" form="fSubAktivitasPengecekanMTsrch" data-ew-action="search-type" data-search-type="=">@Html.Raw(Language.Phrase("QuickSearchExact"))</button>
                <button type="button" class="dropdown-item@(subAktivitasPengecekanMtList.BasicSearch.SessionType == "AND" ? " active" : "")" form="fSubAktivitasPengecekanMTsrch" data-ew-action="search-type" data-search-type="AND">@Html.Raw(Language.Phrase("QuickSearchAll"))</button>
                <button type="button" class="dropdown-item@(subAktivitasPengecekanMtList.BasicSearch.SessionType == "OR" ? " active" : "")" form="fSubAktivitasPengecekanMTsrch" data-ew-action="search-type" data-search-type="OR">@Html.Raw(Language.Phrase("QuickSearchAny"))</button>
            </div>
        </div>
    </div>
    <div class="col-sm-auto mb-3">
        <button class="btn btn-primary" name="btn-submit" id="btn-submit" type="submit">@Html.Raw(Language.Phrase("SearchBtn"))</button>
    </div>
</div>
</div><!-- /.ew-extended-search -->
} @* Show search form (end) *@
}
</div><!-- /.ew-search-panel -->
</form>
}
@subAktivitasPengecekanMtList.ShowPageHeader()
@subAktivitasPengecekanMtList.ShowMessages()
<main class="list @(subAktivitasPengecekanMtList.TotalRecords == 0 && !subAktivitasPengecekanMtList.IsAdd ? " ew-no-record" : "")">
<div id="ew-header-options">
    @subAktivitasPengecekanMtList.HeaderOptions.RenderBody()
</div>
<div id="ew-list">
@if (subAktivitasPengecekanMtList.TotalRecords > 0 || !Empty(subAktivitasPengecekanMtList.CurrentAction)) {
<div class="card ew-card ew-grid@(subAktivitasPengecekanMtList.IsAddOrEdit ? " ew-grid-add-edit" : "") @(subAktivitasPengecekanMtList.TableGridClass)">
<form name="@(subAktivitasPengecekanMtList.FormName)" id="@(subAktivitasPengecekanMtList.FormName)" class="ew-form ew-list-form" action="@AppPath(subAktivitasPengecekanMtList.PageName)" method="post" novalidate autocomplete="off">
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
@if (subAktivitasPengecekanMtList.IsModal) {
<input type="hidden" name="modal" value="1">
}
<div id="gmp_SubAktivitasPengecekanMT" class="card-body ew-grid-middle-panel @(subAktivitasPengecekanMtList.TableContainerClass)" style="@(subAktivitasPengecekanMtList.TableContainerStyle)">
@if (subAktivitasPengecekanMtList.TotalRecords > 0 || subAktivitasPengecekanMtList.IsGridEdit || subAktivitasPengecekanMtList.IsMultiEdit) {
<table id="tbl_SubAktivitasPengecekanMTlist" class="@(subAktivitasPengecekanMtList.TableClass)"><!-- .ew-table -->
<thead>
    <tr class="ew-table-header">
@{
    // Header row
    subAktivitasPengecekanMtList.RowType = RowType.Header;

    // Render list options
    await subAktivitasPengecekanMtList.RenderListOptions();
}
@* Render list options (header, left) *@
@subAktivitasPengecekanMtList.ListOptions.RenderHeader("left")
@if (subAktivitasPengecekanMtList.NoReferensi.Visible) { // subAktivitasPengecekanMtList.NoReferensi.Visible (begin)
        <th data-name="NoReferensi" class="@(subAktivitasPengecekanMtList.NoReferensi.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_NoReferensi" class="SubAktivitasPengecekanMT_NoReferensi">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.NoReferensi))</div></th>
} @* subAktivitasPengecekanMtList.NoReferensi.Visible (end) *@
@if (subAktivitasPengecekanMtList.idProses.Visible) { // subAktivitasPengecekanMtList.idProses.Visible (begin)
        <th data-name="idProses" class="@(subAktivitasPengecekanMtList.idProses.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_idProses" class="SubAktivitasPengecekanMT_idProses">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.idProses))</div></th>
} @* subAktivitasPengecekanMtList.idProses.Visible (end) *@
@if (subAktivitasPengecekanMtList.idAktifitas.Visible) { // subAktivitasPengecekanMtList.idAktifitas.Visible (begin)
        <th data-name="idAktifitas" class="@(subAktivitasPengecekanMtList.idAktifitas.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_idAktifitas" class="SubAktivitasPengecekanMT_idAktifitas">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.idAktifitas))</div></th>
} @* subAktivitasPengecekanMtList.idAktifitas.Visible (end) *@
@if (subAktivitasPengecekanMtList.Produk.Visible) { // subAktivitasPengecekanMtList.Produk.Visible (begin)
        <th data-name="Produk" class="@(subAktivitasPengecekanMtList.Produk.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_Produk" class="SubAktivitasPengecekanMT_Produk">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.Produk))</div></th>
} @* subAktivitasPengecekanMtList.Produk.Visible (end) *@
@if (subAktivitasPengecekanMtList.StatusAktivitas.Visible) { // subAktivitasPengecekanMtList.StatusAktivitas.Visible (begin)
        <th data-name="StatusAktivitas" class="@(subAktivitasPengecekanMtList.StatusAktivitas.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_StatusAktivitas" class="SubAktivitasPengecekanMT_StatusAktivitas">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.StatusAktivitas))</div></th>
} @* subAktivitasPengecekanMtList.StatusAktivitas.Visible (end) *@
@if (subAktivitasPengecekanMtList.LastUpdatedBy.Visible) { // subAktivitasPengecekanMtList.LastUpdatedBy.Visible (begin)
        <th data-name="LastUpdatedBy" class="@(subAktivitasPengecekanMtList.LastUpdatedBy.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_LastUpdatedBy" class="SubAktivitasPengecekanMT_LastUpdatedBy">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.LastUpdatedBy))</div></th>
} @* subAktivitasPengecekanMtList.LastUpdatedBy.Visible (end) *@
@if (subAktivitasPengecekanMtList.lastUpdatedDate.Visible) { // subAktivitasPengecekanMtList.lastUpdatedDate.Visible (begin)
        <th data-name="lastUpdatedDate" class="@(subAktivitasPengecekanMtList.lastUpdatedDate.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_lastUpdatedDate" class="SubAktivitasPengecekanMT_lastUpdatedDate">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.lastUpdatedDate))</div></th>
} @* subAktivitasPengecekanMtList.lastUpdatedDate.Visible (end) *@
@if (subAktivitasPengecekanMtList.NomorPolisi.Visible) { // subAktivitasPengecekanMtList.NomorPolisi.Visible (begin)
        <th data-name="NomorPolisi" class="@(subAktivitasPengecekanMtList.NomorPolisi.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_NomorPolisi" class="SubAktivitasPengecekanMT_NomorPolisi">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.NomorPolisi))</div></th>
} @* subAktivitasPengecekanMtList.NomorPolisi.Visible (end) *@
@if (subAktivitasPengecekanMtList.JamPemeriksaan.Visible) { // subAktivitasPengecekanMtList.JamPemeriksaan.Visible (begin)
        <th data-name="JamPemeriksaan" class="@(subAktivitasPengecekanMtList.JamPemeriksaan.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_JamPemeriksaan" class="SubAktivitasPengecekanMT_JamPemeriksaan">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.JamPemeriksaan))</div></th>
} @* subAktivitasPengecekanMtList.JamPemeriksaan.Visible (end) *@
@if (subAktivitasPengecekanMtList.ExTangki.Visible) { // subAktivitasPengecekanMtList.ExTangki.Visible (begin)
        <th data-name="ExTangki" class="@(subAktivitasPengecekanMtList.ExTangki.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_ExTangki" class="SubAktivitasPengecekanMT_ExTangki">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.ExTangki))</div></th>
} @* subAktivitasPengecekanMtList.ExTangki.Visible (end) *@
@if (subAktivitasPengecekanMtList.Keterangan.Visible) { // subAktivitasPengecekanMtList.Keterangan.Visible (begin)
        <th data-name="Keterangan" class="@(subAktivitasPengecekanMtList.Keterangan.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_Keterangan" class="SubAktivitasPengecekanMT_Keterangan">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.Keterangan))</div></th>
} @* subAktivitasPengecekanMtList.Keterangan.Visible (end) *@
@if (subAktivitasPengecekanMtList.userInput.Visible) { // subAktivitasPengecekanMtList.userInput.Visible (begin)
        <th data-name="userInput" class="@(subAktivitasPengecekanMtList.userInput.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_userInput" class="SubAktivitasPengecekanMT_userInput">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.userInput))</div></th>
} @* subAktivitasPengecekanMtList.userInput.Visible (end) *@
@if (subAktivitasPengecekanMtList.etlDate.Visible) { // subAktivitasPengecekanMtList.etlDate.Visible (begin)
        <th data-name="etlDate" class="@(subAktivitasPengecekanMtList.etlDate.HeaderCellClass)"><div id="elh_SubAktivitasPengecekanMT_etlDate" class="SubAktivitasPengecekanMT_etlDate">@Html.Raw(subAktivitasPengecekanMtList.RenderFieldHeader(subAktivitasPengecekanMtList.etlDate))</div></th>
} @* subAktivitasPengecekanMtList.etlDate.Visible (end) *@
@* Render list options (header, right) *@
@subAktivitasPengecekanMtList.ListOptions.RenderHeader("right")
    </tr>
</thead>
<tbody data-page="@(subAktivitasPengecekanMtList.PageNumber)">
@{
    await subAktivitasPengecekanMtList.SetupGrid();
    bool eof = subAktivitasPengecekanMtList.Recordset == null; // DN
}
@while (subAktivitasPengecekanMtList.RecordCount < subAktivitasPengecekanMtList.StopRecord || SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$")) { // Begin While
    if (!eof && !SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") &&
        (!subAktivitasPengecekanMtList.IsGridAdd || subAktivitasPengecekanMtList.CurrentMode == "copy") &&
        (!((subAktivitasPengecekanMtList.IsCopy || subAktivitasPengecekanMtList.IsAdd) && subAktivitasPengecekanMtList.RowIndex is int rowIndex && rowIndex == 0))) // DN
        eof = !await subAktivitasPengecekanMtList.Recordset.ReadAsync();
    subAktivitasPengecekanMtList.RecordCount++;
    if (subAktivitasPengecekanMtList.RecordCount >= subAktivitasPengecekanMtList.StartRecord) {
        await subAktivitasPengecekanMtList.SetupRow();
    <tr @Html.Raw(subAktivitasPengecekanMtList.RowAttributes)>
    @* Render list options (body, left) *@
    @subAktivitasPengecekanMtList.ListOptions.RenderBody("left", subAktivitasPengecekanMtList.RowCount)
    @if (subAktivitasPengecekanMtList.NoReferensi.Visible) { // subAktivitasPengecekanMtList.NoReferensi.Visible (begin)
        <td data-name="NoReferensi" @Html.Raw(subAktivitasPengecekanMtList.NoReferensi.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_NoReferensi" class="el_SubAktivitasPengecekanMT_NoReferensi">
<span @Html.Raw(subAktivitasPengecekanMtList.NoReferensi.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.NoReferensi.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.NoReferensi.Visible (end) *@
    @if (subAktivitasPengecekanMtList.idProses.Visible) { // subAktivitasPengecekanMtList.idProses.Visible (begin)
        <td data-name="idProses" @Html.Raw(subAktivitasPengecekanMtList.idProses.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_idProses" class="el_SubAktivitasPengecekanMT_idProses">
<span @Html.Raw(subAktivitasPengecekanMtList.idProses.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.idProses.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.idProses.Visible (end) *@
    @if (subAktivitasPengecekanMtList.idAktifitas.Visible) { // subAktivitasPengecekanMtList.idAktifitas.Visible (begin)
        <td data-name="idAktifitas" @Html.Raw(subAktivitasPengecekanMtList.idAktifitas.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_idAktifitas" class="el_SubAktivitasPengecekanMT_idAktifitas">
<span @Html.Raw(subAktivitasPengecekanMtList.idAktifitas.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.idAktifitas.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.idAktifitas.Visible (end) *@
    @if (subAktivitasPengecekanMtList.Produk.Visible) { // subAktivitasPengecekanMtList.Produk.Visible (begin)
        <td data-name="Produk" @Html.Raw(subAktivitasPengecekanMtList.Produk.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_Produk" class="el_SubAktivitasPengecekanMT_Produk">
<span @Html.Raw(subAktivitasPengecekanMtList.Produk.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.Produk.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.Produk.Visible (end) *@
    @if (subAktivitasPengecekanMtList.StatusAktivitas.Visible) { // subAktivitasPengecekanMtList.StatusAktivitas.Visible (begin)
        <td data-name="StatusAktivitas" @Html.Raw(subAktivitasPengecekanMtList.StatusAktivitas.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_StatusAktivitas" class="el_SubAktivitasPengecekanMT_StatusAktivitas">
<span @Html.Raw(subAktivitasPengecekanMtList.StatusAktivitas.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.StatusAktivitas.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.StatusAktivitas.Visible (end) *@
    @if (subAktivitasPengecekanMtList.LastUpdatedBy.Visible) { // subAktivitasPengecekanMtList.LastUpdatedBy.Visible (begin)
        <td data-name="LastUpdatedBy" @Html.Raw(subAktivitasPengecekanMtList.LastUpdatedBy.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_LastUpdatedBy" class="el_SubAktivitasPengecekanMT_LastUpdatedBy">
<span @Html.Raw(subAktivitasPengecekanMtList.LastUpdatedBy.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.LastUpdatedBy.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.LastUpdatedBy.Visible (end) *@
    @if (subAktivitasPengecekanMtList.lastUpdatedDate.Visible) { // subAktivitasPengecekanMtList.lastUpdatedDate.Visible (begin)
        <td data-name="lastUpdatedDate" @Html.Raw(subAktivitasPengecekanMtList.lastUpdatedDate.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_lastUpdatedDate" class="el_SubAktivitasPengecekanMT_lastUpdatedDate">
<span @Html.Raw(subAktivitasPengecekanMtList.lastUpdatedDate.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.lastUpdatedDate.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.lastUpdatedDate.Visible (end) *@
    @if (subAktivitasPengecekanMtList.NomorPolisi.Visible) { // subAktivitasPengecekanMtList.NomorPolisi.Visible (begin)
        <td data-name="NomorPolisi" @Html.Raw(subAktivitasPengecekanMtList.NomorPolisi.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_NomorPolisi" class="el_SubAktivitasPengecekanMT_NomorPolisi">
<span @Html.Raw(subAktivitasPengecekanMtList.NomorPolisi.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.NomorPolisi.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.NomorPolisi.Visible (end) *@
    @if (subAktivitasPengecekanMtList.JamPemeriksaan.Visible) { // subAktivitasPengecekanMtList.JamPemeriksaan.Visible (begin)
        <td data-name="JamPemeriksaan" @Html.Raw(subAktivitasPengecekanMtList.JamPemeriksaan.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_JamPemeriksaan" class="el_SubAktivitasPengecekanMT_JamPemeriksaan">
<span @Html.Raw(subAktivitasPengecekanMtList.JamPemeriksaan.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.JamPemeriksaan.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.JamPemeriksaan.Visible (end) *@
    @if (subAktivitasPengecekanMtList.ExTangki.Visible) { // subAktivitasPengecekanMtList.ExTangki.Visible (begin)
        <td data-name="ExTangki" @Html.Raw(subAktivitasPengecekanMtList.ExTangki.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_ExTangki" class="el_SubAktivitasPengecekanMT_ExTangki">
<span @Html.Raw(subAktivitasPengecekanMtList.ExTangki.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.ExTangki.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.ExTangki.Visible (end) *@
    @if (subAktivitasPengecekanMtList.Keterangan.Visible) { // subAktivitasPengecekanMtList.Keterangan.Visible (begin)
        <td data-name="Keterangan" @Html.Raw(subAktivitasPengecekanMtList.Keterangan.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_Keterangan" class="el_SubAktivitasPengecekanMT_Keterangan">
<span @Html.Raw(subAktivitasPengecekanMtList.Keterangan.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.Keterangan.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.Keterangan.Visible (end) *@
    @if (subAktivitasPengecekanMtList.userInput.Visible) { // subAktivitasPengecekanMtList.userInput.Visible (begin)
        <td data-name="userInput" @Html.Raw(subAktivitasPengecekanMtList.userInput.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_userInput" class="el_SubAktivitasPengecekanMT_userInput">
<span @Html.Raw(subAktivitasPengecekanMtList.userInput.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.userInput.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.userInput.Visible (end) *@
    @if (subAktivitasPengecekanMtList.etlDate.Visible) { // subAktivitasPengecekanMtList.etlDate.Visible (begin)
        <td data-name="etlDate" @Html.Raw(subAktivitasPengecekanMtList.etlDate.CellAttributes)>
<span id="el@(SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$") ? "$rowindex$" : subAktivitasPengecekanMtList.RowCount)_SubAktivitasPengecekanMT_etlDate" class="el_SubAktivitasPengecekanMT_etlDate">
<span @Html.Raw(subAktivitasPengecekanMtList.etlDate.ViewAttributes)>
@Html.Raw(subAktivitasPengecekanMtList.etlDate.GetViewValue())</span>
</span>
</td>
    } @* subAktivitasPengecekanMtList.etlDate.Visible (end) *@
    @* Render list options (body, right) *@
    @subAktivitasPengecekanMtList.ListOptions.RenderBody("right", subAktivitasPengecekanMtList.RowCount)
    </tr>
    } @* End if *@

    // Reset for template row
    if (SameString(subAktivitasPengecekanMtList.RowIndex, "$rowindex$"))
        subAktivitasPengecekanMtList.RowIndex = 0;

    // Reset inline add/copy row
    if ((subAktivitasPengecekanMtList.IsCopy || subAktivitasPengecekanMtList.IsAdd) && subAktivitasPengecekanMtList.RowIndex is int rowIndex2 && rowIndex2 == 0)
        subAktivitasPengecekanMtList.RowIndex = 1;
} @* End While *@
</tbody>
</table><!-- /.ew-table -->
} @* End If TotalRecords > 0 *@
</div><!-- /.ew-grid-middle-panel -->
@if (Empty(subAktivitasPengecekanMtList.CurrentAction) && !subAktivitasPengecekanMtList.UseAjaxActions) {
<input type="hidden" name="action" id="action" value="">
}
</form><!-- /.ew-list-form -->
@{ subAktivitasPengecekanMtList.CloseRecordset(); }
@if (!subAktivitasPengecekanMtList.IsExport()) {
<div class="card-footer ew-grid-lower-panel">
@if (!subAktivitasPengecekanMtList.IsGridAdd && !(subAktivitasPengecekanMtList.IsGridEdit && subAktivitasPengecekanMtList.ModalGridEdit) && !subAktivitasPengecekanMtList.IsMultiEdit) {
@Html.Raw(subAktivitasPengecekanMtList.Pager.Render())
}
<div class="ew-list-other-options">
@subAktivitasPengecekanMtList.OtherOptions.RenderBody("bottom")
</div>
</div>
}
</div><!-- /.ew-grid -->
} else { // Show other options
<div class="ew-list-other-options">
@subAktivitasPengecekanMtList.OtherOptions.ForEach(opt => opt.ButtonClass = "").RenderBody()
</div>
}
</div>
<div id="ew-footer-options">
    @subAktivitasPengecekanMtList.FooterOptions.RenderBody()
</div>
</main>
@subAktivitasPengecekanMtList.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
@if (!subAktivitasPengecekanMtList.IsExport()) {
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("SubAktivitasPengecekanMT");
});
</script>
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
}
