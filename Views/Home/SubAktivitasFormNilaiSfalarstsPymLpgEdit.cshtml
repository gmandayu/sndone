@{
    // View
    View = this;

    // Check action result
    if (subAktivitasFormNilaiSfalarstsPymLpgEdit.ActionResult is EmptyResult) // Terminate page
        return;

    // Layout
    Layout = subAktivitasFormNilaiSfalarstsPymLpgEdit.UseLayout ? "_Layout" : "";
}
@subAktivitasFormNilaiSfalarstsPymLpgEdit.ShowPageHeader()
@subAktivitasFormNilaiSfalarstsPymLpgEdit.ShowMessages()
<main class="edit">
<form name="fSubAktivitasFormNilaiSFALARSTSPymLPGedit" id="fSubAktivitasFormNilaiSFALARSTSPymLPGedit" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.FormClassName)" action="@AppPath(CurrentPageName())" method="post" novalidate autocomplete="off">
<script>
var currentTable = @Html.Raw(ConvertToJson(subAktivitasFormNilaiSfalarstsPymLpgEdit.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { SubAktivitasFormNilaiSFALARSTSPymLPG: currentTable } });
var currentPageID = ew.PAGE_ID = "edit";
var currentForm;
var fSubAktivitasFormNilaiSFALARSTSPymLPGedit;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fSubAktivitasFormNilaiSFALARSTSPymLPGedit")
        .setPageId("edit")

        // Add fields
        .setFields([

            ["NoReferensi", [fields.NoReferensi.visible && fields.NoReferensi.required ? ew.Validators.required(fields.NoReferensi.caption) : null], fields.NoReferensi.isInvalid],
            ["idProses", [fields.idProses.visible && fields.idProses.required ? ew.Validators.required(fields.idProses.caption) : null], fields.idProses.isInvalid],
            ["idAktifitas", [fields.idAktifitas.visible && fields.idAktifitas.required ? ew.Validators.required(fields.idAktifitas.caption) : null], fields.idAktifitas.isInvalid],
            ["StatusAktivitas", [fields.StatusAktivitas.visible && fields.StatusAktivitas.required ? ew.Validators.required(fields.StatusAktivitas.caption) : null], fields.StatusAktivitas.isInvalid],
            ["LastUpdatedBy", [fields.LastUpdatedBy.visible && fields.LastUpdatedBy.required ? ew.Validators.required(fields.LastUpdatedBy.caption) : null], fields.LastUpdatedBy.isInvalid],
            ["lastUpdatedDate", [fields.lastUpdatedDate.visible && fields.lastUpdatedDate.required ? ew.Validators.required(fields.lastUpdatedDate.caption) : null], fields.lastUpdatedDate.isInvalid],
            ["SFAL_mt", [fields.SFAL_mt.visible && fields.SFAL_mt.required ? ew.Validators.required(fields.SFAL_mt.caption) : null], fields.SFAL_mt.isInvalid],
            ["AR_mt", [fields.AR_mt.visible && fields.AR_mt.required ? ew.Validators.required(fields.AR_mt.caption) : null], fields.AR_mt.isInvalid]
        ])

        // Form_CustomValidate
        .setCustomValidate(
            function(fobj) { // DO NOT CHANGE THIS LINE!
                    // Your custom validation code here, return false if invalid.
                    return true;
                }
        )

        // Use JavaScript validation or not
        .setValidateRequired(ew.CLIENT_VALIDATE)

        // Dynamic selection lists
        .setLists({
        })
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Client script
    // Write your client script here, no need to add script tags.
    var parent_url = ew.PATH_BASE;
    var IdProses = "@subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.CurrentValue";
    var NoReferensi = "@subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.CurrentValue";
    let proceedSubmitStatus = null;
    const IdAktivitas = "@subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.CurrentValue";
    $('#x_AR_mt, #x_SFAL_mt').on('focus', function () {
        hideFeedback($(this));
    });
    function hideFeedback(el) {
        let feedback = el.siblings('.invalid-feedback');
        feedback.hide();
    }
    function validasiInput(){
        var inputMap = {
            "#x_AR_mt": "AR Mt",
            "#x_SFAL_mt": "SFAL Mt",
        };
        let isValid = true;
        $('.invalid-feedback').hide();
        let zeroOrNegativeFields = [];
        for (var id in inputMap) {
            var el = $(id);
            var label = inputMap[id];
            var val = el.val().trim();
            el.val(val);
            var feedback = el.siblings('.invalid-feedback');
            if (val === "") {
                feedback.text('*' + label + ' tidak boleh kosong.').show();
                isValid = false;
                continue;
            }
            if (isNaN(val)) {
                feedback.text('*' + label + ' harus diisi dengan angka desimal yang valid.').show();
                isValid = false;
                continue;
            }
            var number = parseFloat(val);
            if (number <= 0) {
                zeroOrNegativeFields.push(label)
            }
        }
        if (!isValid) {
            ew.alert('Nilai input harus berupa angka desimal yang valid.');
            return;
        }
        let fieldNotValid = [];
        for (const fieldId in inputMap) {
            const el = $(fieldId);
            const label = inputMap[fieldId];
            if (el.length && !el.val().trim()) {
                fieldNotValid.push(label);
                isValid = false
            }
        }
        if (!isValid) {
            ew.alert("Field tidak boleh kosong: " + fieldNotValid.join(", "));
            return { isValid: false}; // Gagal submit
        } else {
            $('.invalid-feedback').hide();
            if (zeroOrNegativeFields.length > 0) {
                return { isValid: false, hasNegative: true, fields: zeroOrNegativeFields };
            }
                return { isValid: true};
        }
        return { isValid: true};
    }
    function handleCustomSubmit(status) {
        let idAktivitas = $("#x_idAktifitas").val();
        const originalStatus = document.querySelector('#el_SubAktivitasFormNilaiSFALARSTSPymLPG_StatusAktivitas .form-control-plaintext').textContent;
        const isValidInput = validasiInput();
        if (!isValidInput.isValid && !isValidInput.hasNegative) return;
        let allNegativeFields = [];
        if (isValidInput.hasNegative) allNegativeFields = allNegativeFields.concat(isValidInput.fields);
        if (allNegativeFields.length > 0) {
            let warningText = "Field berikut bernilai 0 atau kurang: " + allNegativeFields.join(", ") + ". Apakah Anda ingin tetap menyimpan?";
            $("#warningValueText").text(warningText);
            proceedSubmitStatus = status;
            $("#warningValueModal").modal("show");
            return;
        }
        submitFinal(status);
    }
    function submitFinal(status) {
        let idAktivitas = $("#x_idAktifitas").val();
        const originalStatus = document.querySelector('#el_SubAktivitasFormNilaiSFALARSTSPymLPG_StatusAktivitas .form-control-plaintext').textContent;
        $.ajax({
            url: parent_url + `api/LookUpList/check_valid_status_aktivitas?idAktivitas=${idAktivitas}&originalStatus=${originalStatus}`,
            type: 'GET',
            beforeSend: function () {
                showLoading();
            },
            success: function (response) {
                if (!response.IsValid){
                    hideLoading();
                    ew.alert(response.Message ?? "Status aktivitas telah diubah oleh pengguna lain");
                    return;
                } else {
                    let form = $("#fSubAktivitasFormNilaiSFALARSTSPymLPGedit");
                    $.ajax({
                        url: form.attr("action"),
                        type: form.attr("method"),
                        data: form.serialize(),
                        success: function (response) {
                            updateStatus(idAktivitas, status);
                        },
                        error: function (xhr, status, error) {
                            hideLoading();
                            ew.alert("Gagal menyimpan data.");
                            console.error("Form submit gagal:", error);
                        }
                    });  
                }
            },
            error: function (xhr, status, error) {
                hideLoading();
                ew.alert("Gagal menyimpan data.");
                console.error("Form submit gagal:", error);
            }
        }); 
    }
    function updateStatus(idAktivitas, status) {
        $.ajax({
            url: parent_url + `api/LookUpList/update_status_aktivitas?idAktivitas=${idAktivitas}&statusAktivitas=${status}`,
            type: 'POST',
            success: function () {
                $.ajax({
                    url: parent_url + 'api/LookUpList/update_dokumen_over_toleransi',
                    type: 'POST',
                    data: JSON.stringify({
                        NoReferensi: NoReferensi,
                        IdProses: IdProses,
                        IdAktivitas: IdAktivitas,
                        TipeProduk: 'LPG'
                    }),
                    contentType: 'application/json',
                    success: function (res) {
                        if (status == "Completed" || status == "Completed Edit") {
                            window.location.href = `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`;
                        } else {
                            location.reload();
                        }
                    },
                    error: function (xhr, status, error) {
                        console.error("Update Aktivitas Dokumen gagal:", error);
                    },
                    complete: function () {
                        hideLoading();
                    }
                });
            },
            error: function (xhr, status, error) {
                ew.alert("Status gagal diperbarui.");
                console.error("Update status gagal:", error);
            },
            complete: function () {
                hideLoading();
            }
        });
    }
    function reverseStatus(status, catatan) {
        showLoading();
        let idAktivitas = $("#x_idAktifitas").val();
        $.ajax({
            url: parent_url + `api/LookUpList/update_status_aktivitas-v2`,
            type: 'POST',
            data: JSON.stringify({ IdAktivitas: idAktivitas, StatusAktivitas: status, Catatan: catatan ?? null }),
            contentType: 'application/json',
            success: function () {
                setTimeout(function () {
                    window.location.href = `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`;
                }, 1000);
            },
            error: function (xhr, status, error) {
                ew.alert("Status gagal diperbarui.");
                console.error("Update status gagal:", error);
            },
            complete: function () {
                hideLoading();
            }
        });
    }
    $(document).on("click", "#btn-draft", function () {
        handleCustomSubmit("Draft");
    });
    $(document).on("click", "#btn-completed", function () {
        handleCustomSubmit("Completed");
    });
    $(document).on("click", "#btn-editing", function () {
        handleCustomSubmit("Editing");
    });
    $(document).on("click", "#btn-completedEdit", function () {
        handleCustomSubmit("Completed Edit");
    });
    $(document).on("click", "#btn-confirm-modal", function () {
        $("#warningValueModal").modal("hide");
        submitFinal(proceedSubmitStatus);
    });
    $(document).on("submit", "#reverseStatusEditingModalForm", function (e) {
        e.preventDefault();
        let catatanText = $("#catatanReverseStatusEditing").val();
        reverseStatus("Editing", catatanText);
    });
});
</script>
@if (CurrentPage?.CheckToken ?? false) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="update">
<input type="hidden" name="modal" value="@ConvertToInt(subAktivitasFormNilaiSfalarstsPymLpgEdit.IsModal)">
@if (IsJsonResponse()) {
<input type="hidden" name="json" value="1">
}
<input type="hidden" name="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.OldKeyName)" value="@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.OldKey)">
<div class="ew-edit-div"><!-- page* -->
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.Visible (begin)
    <div id="r_NoReferensi" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_NoReferensi" for="x_NoReferensi" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_NoReferensi">
<span @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_NoReferensi" data-hidden="1" name="x_NoReferensi" id="x_NoReferensi" value="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.Visible (end) *@
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.Visible (begin)
    <div id="r_idProses" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_idProses" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_idProses">
<span @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.DisplayValue(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_idProses" data-hidden="1" name="x_idProses" id="x_idProses" value="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.Visible (end) *@
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.Visible (begin)
    <div id="r_idAktifitas" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_idAktifitas" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_idAktifitas">
<span @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.DisplayValue(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_idAktifitas" data-hidden="1" name="x_idAktifitas" id="x_idAktifitas" value="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.Visible (end) *@
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.Visible (begin)
    <div id="r_StatusAktivitas" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_StatusAktivitas" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_StatusAktivitas">
<span @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.ViewAttributes)>
<span class="form-control-plaintext">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.DisplayValue(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.EditValue))</span></span>
<input type="hidden" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_StatusAktivitas" data-hidden="1" name="x_StatusAktivitas" id="x_StatusAktivitas" value="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.StatusAktivitas.Visible (end) *@
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.Visible (begin)
    <div id="r_LastUpdatedBy" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_LastUpdatedBy" for="x_LastUpdatedBy" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_LastUpdatedBy">
<span @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_LastUpdatedBy" data-hidden="1" name="x_LastUpdatedBy" id="x_LastUpdatedBy" value="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.LastUpdatedBy.Visible (end) *@
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.Visible (begin)
    <div id="r_lastUpdatedDate" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_lastUpdatedDate" for="x_lastUpdatedDate" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_lastUpdatedDate">
<span @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.ViewAttributes)>
<input type="text" readonly class="form-control-plaintext" value="@Html.Raw(RemoveHtml(ConvertToString(subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.EditValue)))"></span>
<input type="hidden" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_lastUpdatedDate" data-hidden="1" name="x_lastUpdatedDate" id="x_lastUpdatedDate" value="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.CurrentValue))">
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.lastUpdatedDate.Visible (end) *@
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.Visible (begin)
    <div id="r_SFAL_mt" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_SFAL_mt" for="x_SFAL_mt" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_SFAL_mt">
<input type="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.InputTextType)" name="x_SFAL_mt" id="x_SFAL_mt" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_SFAL_mt" value="@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.EditValue)" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.PlaceHolder))" data-format-pattern="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.FormatPattern)"@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.EditAttributes) aria-describedby="x_SFAL_mt_help">
@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.GetErrorMessage())</div>
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.SFAL_mt.Visible (end) *@
@if (subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.Visible) { // subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.Visible (begin)
    <div id="r_AR_mt" @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.RowAttributes)>
        <label id="elh_SubAktivitasFormNilaiSFALARSTSPymLPG_AR_mt" for="x_AR_mt" class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.LeftColumnClass)">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.Caption)@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.RightColumnClass)"><div @Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.CellAttributes)>
<span id="el_SubAktivitasFormNilaiSFALARSTSPymLPG_AR_mt">
<input type="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.InputTextType)" name="x_AR_mt" id="x_AR_mt" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_AR_mt" value="@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.EditValue)" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.PlaceHolder))" data-format-pattern="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.FormatPattern)"@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.EditAttributes) aria-describedby="x_AR_mt_help">
@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.GetErrorMessage())</div>
</span>
</div></div>
    </div>
} @* subAktivitasFormNilaiSfalarstsPymLpgEdit.AR_mt.Visible (end) *@
</div><!-- /page* -->
    <input type="hidden" data-table="SubAktivitasFormNilaiSFALARSTSPymLPG" data-field="x_id" data-hidden="1" name="x_id" id="x_id" value="@Html.Raw(HtmlEncode(subAktivitasFormNilaiSfalarstsPymLpgEdit.id.CurrentValue))">
@{ subAktivitasFormNilaiSfalarstsPymLpgEdit.CloseRecordset(); }
@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.IsModal ? "<template class=\"ew-modal-buttons\">" : "<div class=\"row ew-buttons\">")<!-- buttons .row -->
    <div class="@(subAktivitasFormNilaiSfalarstsPymLpgEdit.OffsetColumnClass)"><!-- buttons offset -->
    <button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit" form="fSubAktivitasFormNilaiSFALARSTSPymLPGedit">@Html.Raw(Language.Phrase("SaveBtn"))</button>
@if (IsJsonResponse()) {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-bs-dismiss="modal">@Html.Raw(Language.Phrase("CancelBtn"))</button>
} else {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" form="fSubAktivitasFormNilaiSFALARSTSPymLPGedit" data-href="@AppPath(subAktivitasFormNilaiSfalarstsPymLpgEdit.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
}
    </div><!-- /buttons offset -->
@Html.Raw(subAktivitasFormNilaiSfalarstsPymLpgEdit.IsModal ? "</template>" : "</div>")<!-- /buttons .row -->
</form>
</main>
@subAktivitasFormNilaiSfalarstsPymLpgEdit.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("SubAktivitasFormNilaiSFALARSTSPymLPG");
});
</script>
<script>
loadjs.ready("load", function() {
    // Startup script
    var IdReferensi = "@subAktivitasFormNilaiSfalarstsPymLpgEdit.NoReferensi.CurrentValue";
    var IdProses = "@subAktivitasFormNilaiSfalarstsPymLpgEdit.idProses.CurrentValue";
    var idAktivitas = "@subAktivitasFormNilaiSfalarstsPymLpgEdit.idAktifitas.CurrentValue";
    var NamaAktivitas = $('#el_SubAktivitasFormNilaiSFALARSTSPymLPG_idAktifitas').text();
    let StatusAktivitas = "";
    const userLevel = "@CurrentUserLevel()";
    var parent_url = ew.PATH_BASE;
    var steps = [
        { number: 1, label: "Proses Penerimaan", url: `${parent_url}PenimbunanList`, icon: "fa-oil-can" },
        { number: 2, label: "Proses", url: `${parent_url}ProsesList?cmd=search&x_IdReferensi%5B%5D=${IdReferensi}&search=&searchtype=`, icon: "fa-gears" },
        { number: 3, label: "Aktivitas", url: `${parent_url}AktivitasList?cmd=search&x_IdProses%5B%5D=${IdProses}&search=&searchtype=`, icon: "fa-list-check" },
        { number: 4, label: NamaAktivitas, url: '', icon: "fa-file-circle-check" }
    ];
    var currentStep = 4;
    $("<style>")
        .prop("type", "text/css")
        .html(`
            .stepper {
                display: flex;
                counter-reset: step;
                flex-wrap: nowrap;        /* jangan pecah ke bawah */
                overflow-x: auto;         /* scroll horizontal */
                -webkit-overflow-scrolling: touch; /* smooth scroll di mobile */
                padding-bottom: 8px;      /* space biar scroll bar gak nempel */
            }
            .stepper::-webkit-scrollbar {
            height: 6px; /* tipis */
            }
            .stepper::-webkit-scrollbar-thumb {
                background-color: #ccc;
                border-radius: 3px;
            }
            .step {
                width: 150px;
                text-align: center;
                position: relative;
            }
            .step:not(:last-child)::after {
                content: '';
                position: absolute;
                top: 20px;
                right: -50%;
                width: 100%;
                height: 4px;
                background-color: #ccc;
                z-index: 0;
            }
            .step.completed:not(:last-child)::after {
                background-color: #3c6dbd;
            }
            .step-icon {
                width: 36px;
                height: 36px;
                line-height: 36px;
                border-radius: 50%;
                background: #ccc;
                display: inline-block;
                z-index: 1;
                position: relative;
                color: white;
                font-weight: bold;
            }
            .step.active .step-icon {
                background: #3c6dbd;
            }
            .step-label {
                margin-top: 8px;
                font-size: 14px;
                display: flex;
                justify-content: center;
                align-items: center;
                gap: 6px;
                color: #333;
            }
            .step-label {
                color: #3c6dbd !important;
                text-decoration: none;
                margin: 0;
                padding: 0;
            }
            .step-label-not-active {
                color: #CCCCCC !important;
            }
            .ag-theme-quartz .ag-header {
                background-color: #3c6dbd !important;
            }
            .ag-theme-quartz .ag-header-cell-label {
                color: #ffffff !important;
            }
            .ag-theme-quartz .ag-icon-filter {
                color: #ffffff !important;
            }
            .rotate-down {
                transform: rotate(90deg);
                transition: transform 0.3s ease;
            }
            .rotate-right {
                transform: rotate(0deg);
                transition: transform 0.3s ease;
            }
            .header-table {
                background-color: #3c6dbd !important;
                color: #fff !important;
            }
            .newblsfad{
                width: 15%;
            }
            .ar45b{
                width: 15%;
            }
            #table_bl_sfad_api table {
                width: 100%;             
                table-layout: fixed;    
                max-width: 850px;        
            }
        `)
        .appendTo("head");
    var $stepperWrapper = $('<div class="d-flex border-bottom border-2 pb-2"><div class="stepper"></div></div>');
    var $stepper = $stepperWrapper.find(".stepper");
    steps.forEach(function (step, i) {
        var isActive = step.number === currentStep;
        var isCompleted = step.number < currentStep;
        var stepClass = isActive ? "active" : isCompleted ? "completed" : "";
        var icon, labelContent, html;
        if (isActive) {
            icon = `<div class="step-icon bg-success">${step.number}</div>`;
            labelContent = `<div class="step-label"><p class="text-success mb-1"><i class="fa-solid ${step.icon} me-1 text-success"></i> ${step.label}</p></div>`;
            html = `<div class="step ${stepClass} text-success">${icon}${labelContent}</div>`;
        } else if (isCompleted) {
            icon = `<div class="step-icon" style="background: #3c6dbd;">${step.number}</div>`;
            labelContent = `<div class="step-label"><a href="${step.url}" class="text-primary"><i class="fa-solid ${step.icon} me-1"></i>${step.label}</a></div>`;
            html = `<div class="step ${stepClass}">${icon}${labelContent}</div>`;
        } else {
            icon = `<div class="step-icon">${step.number}</div>`;
            labelContent = `<div class="step-label-not-active"><p style="margin: 0;"><i class="fa-solid ${step.icon} me-1"></i>${step.label}</p></div>`;
            html = `<div class="step ${stepClass} text-secondary">${icon}${labelContent}</div>`;
        }
        $stepper.append(html);
    });
    $(".content-header .container-fluid .row").append($stepperWrapper);
    $("#btn-action").hide();
    $("#btn-cancel").html('<i class="fa-solid fa-ban"></i></i> Cancel');
    const $cancelButton = $("#btn-cancel");
    const idAktifitas = $("#x_idAktifitas").val();

    // --- log aktivitas start ---
    $('#el_SubAktivitasFormNilaiSFALARSTSPymLPG_LastUpdatedBy').addClass('d-flex gap-3');
    $('#el_SubAktivitasFormNilaiSFALARSTSPymLPG_LastUpdatedBy').append(`
        <button type="button" class="btn btn-primary ew-btn ps-2" expand-log="false">
            <i class="fa-solid fa-caret-right fa-2xl me-2 caret-icon rotate-right"></i>
            Log History
            <i class="fa-solid fa-clock-rotate-left ms-1"></i> 
        </button>
    `);
    $(document).on('click', 'button[expand-log]', function () {
        const $btn = $(this);
        const isExpanded = $btn.attr('expand-log') === 'true';
        const $row = $('#log-table-row');
        const $wrapper = $('#log-table-wrapper');
        const $caret = $btn.find('.caret-icon');
        if (!isExpanded) {
            $btn.attr('expand-log', 'true');
            $row.removeClass('d-none');
            setTimeout(() => {
                $wrapper.css({
                    maxHeight: '500px'
                });
            }, 10);
            $caret.removeClass('rotate-right').addClass('rotate-down');
        } else {
            $btn.attr('expand-log', 'false');
            $wrapper.css({
                maxHeight: '0'
            });
            setTimeout(() => {
                $row.addClass('d-none');
            }, 600);
            $caret.removeClass('rotate-down').addClass('rotate-right');
        }
    });
    if (idAktivitas && idAktivitas !== "0") {
        $.ajax({
            url: parent_url + `api/LookUpList/sub-aktivitas/startup?idAktivitas=${idAktivitas}`,
            type: 'GET',
            success: function(response) {
                // cek status
                if ($cancelButton.length){
                    let status = response.StatusAktivitas;
                    currentStatusAktivitas = status;
                    let $customButtons = ``;
                    switch (status) {
                        case "Waiting":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-draft">
                                    <i class="fas fa-save"></i> Save
                                </button>
                            `;
                            break;
                        case "Draft":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-draft">
                                    <i class="fas fa-save"></i> Save
                                </button>
                                <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completed">
                                    <i class="fas fa-paper-plane"></i> Submit
                                </button>
                            `
                            break;
                        case "Completed":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-editing">
                                    <i class="fa-solid fa-pen-to-square"></i> Edit
                                </button>
                                <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completedEdit">
                                    <i class="fas fa-paper-plane"></i> Submit
                                </button>
                            `;
                            break;
                        case "Editing":
                            $customButtons = `
                                <button type="button" class="btn btn-primary ew-btn ml-2" id="btn-editing">
                                    <i class="fa-solid fa-pen-to-square"></i> Edit
                                </button>
                                <button type="button" class="btn btn-success ew-btn ml-2" id="btn-completedEdit">
                                    <i class="fas fa-paper-plane"></i> Submit
                                </button>
                            `;
                            break;
                        case "Completed Edit":
                            if (userLevel === '4'){
                                $customButtons = `
                                    <button type="button" class="btn btn-warning ew-btn ml-2" id="btn-reverse-status">
                                        <i class="fa-solid fa-pen-to-square"></i> Edit 
                                    </button>
                                `;
                            }
                            break;
                    }
                    $cancelButton.before($customButtons);
                }

                // Cek is editable
                if(!response.IsEditable || response.StatusAktivitas === "Completed Edit"){
                    $("#btn-draft").hide();
                    $("#btn-editing").hide();
                    $("#btn-completed").hide();
                    $("#btn-completedEdit").hide();
                    $('input, select, textarea').prop('disabled', true);
                    $('#catatanReverseStatusEditing').prop('disabled', false);
                }

                // Log
                const mappedData = response.LogAktivitas.map(item => ({
                    Aktivitas: item.Aktivitas,
                    Username: item.UserName,
                    StatusAktivitas: item.StatusAktivitas,
                    Tanggal: item.Tanggal,
                    Catatan: item.Catatan
                }));
                $.getScript('https://cdn.jsdelivr.net/npm/ag-grid-community/dist/ag-grid-community.min.js', function () {
                        $('#r_LastUpdatedBy').after(`
                            <div id="log-table-row" class="d-none row m-0">
                                <div class="p-0 col-2"></div>
                                <div class="p-0 col-10">
                                    <div id="log-table-wrapper" style="overflow: hidden; transition: all 0.6s ease; max-height: 0;">
                                        <div id="myGrid" class="ag-theme-quartz w-100 my-3" style="height: 300px;"></div>
                                    </div>
                                </div>
                            </div>
                        `);
                        const gridOptions = {
                            rowData: mappedData,
                            columnDefs: [
                                {
                                    headerName: 'No.',
                                    valueGetter: 'node.rowIndex + 1',
                                    width: 80,
                                    filter: false
                                },
                                {
                                    headerName: 'Aktivitas',
                                    field: 'Aktivitas',
                                    flex: 1
                                },
                                {
                                    headerName: 'Username',
                                    field: 'Username',
                                    flex: 1
                                },
                                {
                                    headerName: 'Status Aktivitas',
                                    field: 'StatusAktivitas',
                                    flex: 1
                                },
                                {
                                    headerName: 'Tanggal',
                                    field: 'Tanggal',
                                    flex: 1
                                },
                                {
                                    headerName: 'Catatan',
                                    field: 'Catatan',
                                    flex: 1
                                }
                            ],
                            defaultColDef: {
                                sortable: false,
                                suppressMovable: true,
                                filter: true,
                                resizable: false
                            },
                            rowSelection: 'multiple',
                            suppressRowClickSelection: true,
                            pagination: true,
                            paginationPageSize: 10
                        };
                        const myGridElement = document.querySelector('#myGrid');
                        agGrid.createGrid(myGridElement, gridOptions);
                    });
            },
            error: function(xhr, status, error) {
                ew.alert(xhr.responseJSON.message ?? "Terjadi kesalahan dalam memuat data");
            }
        });
    } else {
        console.warn("Skipping startup API: Invalid IdAktivitas");
        $('button[expand-log]').hide(); 
        $("#btn-draft, #btn-editing, #btn-completed, #btn-completedEdit").hide();
        $('input, select, textarea').prop('disabled', true);
    }
    // --- log aktivitas end ---
    const tableHtml = `
    <div class="d-flex flex-column flex-md-row w-100 gap-4">
        <div id="allValues" class="w-100">
            <div>
                <div id="table_bl_sfad_api" class="table-responsive border rounded-3 mb-4" style="max-width: 850px;">
                    <table class="table rounded-4 m-0">
                        <thead>
                            <tr style="height: 50px;">
                                <th class="fw-bold header-table">Satuan</th>
                                <th class="fw-bold header-table">BL</th>
                                <th class="fw-bold header-table">SFAL</th>
                                <th class="fw-bold header-table">SFBD</th>
                                <th class="fw-bold header-table">SFAL</th>
                                <th class="fw-bold header-table">SFBL</th>
                                <th class="fw-bold header-table">AR</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr style="height: 58px;">
                                <td><strong>Metric Ton</strong></td>
                                <td id="td_BL_Mt_api"></td>
                                <td id="td_SFAL_Mt_api"></td>
                                <td id="td_SFBD_Mt_api"></td>
                                <td id="td_SFAL_Mt"></td>
                                <td id="td_SFBL_Mt_api"></td>
                                <td id="td_AR_Mt"></td>
                            </tr>
                        </tbody>
                    </table>
                </div>           
            </div>
            <table aria-hidden="true" class="table table-hover view-page-table w-50 mb-3">
                <tbody>
                    <tr>
                        <td class="text-break fw-bold">R1</td>
                        <td class="text-break body-table fw-bold" id="R1"></td>
                    </tr>
                    <tr>
                        <td class="text-break fw-bold">R2</td>
                        <td class="text-break body-table fw-bold" id="R2"></td>
                    </tr>
                    <tr>
                        <td class="text-break fw-bold">R3</td>
                        <td class="text-break body-table fw-bold" id="R3"></td>
                    </tr>
                    <tr>
                        <td class="text-break fw-bold">R4</td>
                        <td class="text-break body-table fw-bold" id="R4"></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
        `;
    $("#r_lastUpdatedDate").after(tableHtml);
    const moveField = (fieldId, targetTdId) => {
        const fieldRow = $("#" + fieldId).closest(".row");
        $("#" + fieldId).appendTo("#" + targetTdId); // move field to table
        fieldRow.hide(); // hide original
    };
    moveField("el_SubAktivitasFormNilaiSFALARSTSPymLPG_SFAL_mt", "td_SFAL_Mt");
    moveField("el_SubAktivitasFormNilaiSFALARSTSPymLPG_AR_mt", "td_AR_Mt");
    var r4Value = 0;
    var r4ImportValue = 0;
    function toNumber(value) {
        if (value === null || value === undefined) return 0;
        if (typeof value === "string") value = value.trim();
        const num = parseFloat(value);
        return isNaN(num) ? 0 : num;
    }  
    let sfblData = {};
    function updateAR() {
        const sfblMt      = sfblData.mt      ?? 0;
        const sfalMt      = toNumber($('#x_SFAL_mt').val());
        const arMt      = sfalMt      - sfblMt;
        $("#x_AR_mt").val(arMt).prop("readonly", true);
    }
    $.ajax({
            url: parent_url + `api/LookUpList/GetNomorPenerimaanFromPenimbunan?NomorPenimbunan=${IdReferensi}`,
            method: "GET",
            success: function (res) {
                if (res.success) {
                    const noReferensi = res.nomorPenerimaan;

                    // Bl
                    $.get(parent_url + `api/LookUpList/GetSFBLValue?NoReferensi=${IdReferensi}&Jenis=STSLPG`, function (data) {
                    if (data.success) {
                        sfblData = {
                            mt:      toNumber(data.mt),
                        };

                        // tampilkan ke tabel
                        $("#td_SFBL_Mt_api").text(sfblData.mt);

                        // hitung pertama kali
                        updateAR();
                    }
                });

                // SFAL
                $.get(parent_url + `api/LookUpList/GetBLSFALValue?NoReferensi=${noReferensi}&Jenis=STSLPG`, function (data) {
                    if (data.success) {
                        // BL
                        $("#td_BL_Mt_api").text(data.bl_mt);
                        // SFAL
                        $("#td_SFAL_Mt_api").text(data.sfal_mt);
                    }
                });

                // SFBD
                $.get(parent_url + `api/LookUpList/GetSFBDValue?NoReferensi=${noReferensi}&Jenis=STSLPG`, function (data) {
                    if (data.success) {
                        $("#td_SFBD_Mt_api").text(data.mt);
                    }
                });
                $.ajax({
                    url: parent_url + "api/LookUpList/GetRValue",
                    type: "GET",
                    data: {
                        NoReferensi: IdReferensi,
                        Jenis: "STSLPG",
                        Menu: "AR"
                    },
                    success: function(rs){
                        if (rs.success) {

                            ["R1", "R2", "R3", "R4"].forEach(key => {
                                let val = parseFloat(rs[key]);
                                const el = document.getElementById(key);
                                if (!isNaN(val)) {
                                    let color = "";
                                    val = val * 100;
                                    if (key === "R2") {
                                        if (val < -0.125) {
                                            color = "#ba313b";
                                        } else if (val >= -0.125 && val <= 0) {
                                            color = "#3c6dbd";
                                        } else {
                                            color = "#198754";
                                        }
                                    } else {
                                        if (key == "R4"){
                                            r4Value = val;
                                            const isChecked = $("#x_ApakahTerdapatROB").prop("checked");
                                            if (isChecked){
                                                val = 0;
                                            }
                                        }
                                        if (val < -0.125) {
                                            color = "#ba313b";
                                        } else if (val >= -0.125 && val <= 0) {
                                            color = "#3c6dbd";
                                        } else {
                                            color = "#198754";
                                        }
                                    }
                                    el.textContent = val.toFixed(3) + "%";
                                    el.style.color = color;
                                }
                            });
                        }
                    }
                });    
                } else {
                    alert("Gagal mendapatkan nomor referensi: " + res.message);
                }
            }
        });

    // Pasang listener supaya setiap perubahan SFAL langsung update AR
    $("#x_SFAL_mt")
        .on("input change", function () {
            updateAR();
        });
    let uploadModal = `<div class="modal fade" id="warningValueModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="modal-title fs-5" id="warningValueModalTitle">Konfirmasi Simpan Data</h1>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body" id='warningValueText'>
            Konfirmasi 
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" id="btn-cancel-modal">Batal</button>
            <button type="button" class="btn btn-primary" id="btn-confirm-modal">Lanjutkan</button>
          </div>
        </div>
      </div>
    </div>`
    $("body").append(uploadModal);
    $("#btn-cancel-modal").click(function (event) {
        event.preventDefault();
        $("#warningValueModal").modal("hide");
    });
    $('#x_SFAL_mt')
        .attr('inputmode', 'numeric')
        .on('input', function () {
            let val = $(this).val();
            let isNegative = val.startsWith('-');
            val = val.replace(',', '.');
            val = val.replace(/[^0-9.]/g, '');
            const parts = val.split('.');
            if (parts.length > 2) {
                val = parts[0] + '.' + parts.slice(1).join('');
            }
            if (isNegative) {
                val = '-' + val;
            }
            $(this).val(val);
        });

                // Cari elemen container dengan id r_Tujuan
            var el = document.getElementById("r_lastUpdatedDate");
            if (el) {
                // Buat elemen note
                var note = document.createElement("div");
                note.innerHTML = `
                <div class="mt-2">
                  <div class="card border-primary shadow-sm" style="max-width: 850px;">
                    <div class="card-header bg-primary text-white py-2 px-3">
                          <strong>Informasi Pengisian AR</strong>
                    </div>
                    <div class="card-body py-2 px-3 text-dark">
                      <p class="mb-2">
                      Nilai <strong>AR (Metric Ton)</strong> tidak diinput langsung pada form ini.
                            Pengisian dilakukan melalui menu <strong>Penyimpanan</strong>, kemudian pilih bagian <strong>Proses Penerimaan</strong>, dengan menyesuaikan <strong>Nomor Referensi Penerimaan yang sama.</strong>
                      </p>
                    </div>
                  </div>
                </div>
        `;
                note.style.marginTop = "5px";

                // Sisipkan setelah elemen
                el.parentNode.insertBefore(note, el.nextSibling);
            }

        // Reverse Status to Editing
            let reverseStatusModal = `<div class="modal fade" id="confirmReverseStatusEditingModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-header bg-primary">
                    <h1 class="modal-title fs-5" id="confirmReverseStatusEditingModalTitle">Konfirmasi Kembalikan Status</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                  </div>
                  <form id="reverseStatusEditingModalForm">
                    <div class="modal-body px-2 py-1">
                        <div class="mb-3">
                            <label for="message-text" class="col-form-label">Catatan:</label>
                            <textarea class="form-control" id="catatanReverseStatusEditing" required rows="4"></textarea>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" id="btnReverseStatusEditingModalCancel">Batal</button>
                        <button type="submit" class="btn btn-primary" id="btnReverseStatusEditingModalSubmit">Lanjutkan</button>
                    </div>
                  </form>
                </div>
              </div>
            </div>`
    $("body").append(reverseStatusModal);
    $("#btnReverseStatusEditingModalCancel").click(function (event) {
        event.preventDefault();
        $("#confirmReverseStatusEditingModal").modal("hide");
    });
    $(document).on("click", "#btn-reverse-status", function () {
        $("#confirmReverseStatusEditingModal").modal("show");
    });
});
</script>
